# CHANGELOG

遵循北极星：每个版本都是一个能独立交付价值的完整切片。

> **本文件曾从 0.3.0 起停更约 10 个切片**（2026-06-28 → 2026-09-20）。
> 那段历史不在这里，在 [EVOLUTION.md](EVOLUTION.md)（逐代评估 + 证据分级）与
> [docs/RECAP_2026-09-20.md](docs/RECAP_2026-09-20.md)（全景 + 侧批）。
> 下面的「未记录期」条目只做索引，不假装当时写过。

## 0.17.0 — 会话驾驶舱 S1：`/sessions` 一行看全四件事

**动机**：北极星要随时回答 **推进 / 卡住 / 烧钱 / 乱改**。`/sessions` 是最常用的页，状态（0.15.1 起进程自报）和风险角标（0.15.0）
都有了，**烧钱一个数都没有**——要看钱得切到 `/tokens` 或点进回放。规格见 [SESSIONS_COCKPIT_PLAN.md](SESSIONS_COCKPIT_PLAN.md)。

**交付**
- **每行**：行尾是当前任务的**等价 $**（各行一列对齐着扫）；有新消耗时多一枚「+X/10分」——近 10 分钟新增的 token；
  副标题加「本任务 N tok · 活跃 M · ✎ K 个文件」。token 是 transcript 里 API usage 去重后的**真值**，
  有 agent / workflow 文件缺失时写「≥」（下限）；$ 与 `/workflow`、`/tokens` 同一套单价，写「≈」（估算，不是账单），未知单价打 *。
- **页顶**：在状态计数后面加三枚事实：**今天已烧**（与 Token 看板「今天 · 全部」同一口径，点开就是那一页）·
  **近 10 分钟**所有会话新增的 token · **有风险**的会话数。
- 「近 10 分钟」在请求时现算：简报里存的是每次 API 响应的（时刻, token），缓存的是序列不是结果，文件不再变化时窗口照样往前滑。
- **简报不再阻塞请求**：`/api/sessions` 只读缓存（可以旧一点），没有或过期的交给一个后台线程，正在跑的会话排在前面；
  同一个会话文件加锁，风险 pump 和页面不会重复解析。还没算出来的在页面上写明「还有 N 个在后台计算」。
- **重启后不再空窗**：简报不再等全量耗时基线（简报里的数都用不到「偏慢」）。
- 顺手：统一导航在手机宽度下不再一个字一个字地折行（导航项整体换行）。
- **S0（同一轨的前置重构，无版本号）**：9 个内联页面搬出 `serve.py` 到 `tokmon/pages/*.html`，搬前搬后逐字节一致，
  `serve.py` 2281 → 约 1100 行；新测试守着不许再内联。

**完成判据**
- ① **每行 token 与回放页头逐个对账**：真实数据上 24 小时内有动静的 13 个会话，行里的 token、等价 $、「≥」标记
  与 `/api/workflow/task` 返回的回放页头**13/13 全等**。单测里同样钉死（`tests/test_trends.py`）。
- ② 真实会话上截图核对（桌面 1500px + 手机 390px）：页顶三枚事实、行尾 $、「+X/10分」、副标题都在，无 `undefined` / `NaN`。
- ③ **实测耗时**（本机，服务刚重启）：

| 场景 | 耗时 |
|---|---|
| 重启后第一次 `/api/sessions` | 1.27s（大部分花在「今天已烧」第一次扫全部记录，之后 30 秒复用） |
| 正在跑的会话出现花费与角标 | 重启后 13.6s |
| 13 个会话的简报全部算完 | 重启后 55s（三个大会话占了大头） |
| 之后每次刷新 | 0.015–0.19s |

  **自测中发现并改掉的**：第一版去掉基线等待后，让请求当场解析全部会话——重启后第一次打开页面要白等 **50 秒**，
  比原来「十几秒没角标」更糟；改成后台线程 + 只读缓存。

测试 482 → 486。

## 0.16.0 — `/tokens` 重做：一屏分析台（驾驶舱版）

> 代码早于 0.15.1 并入 main（`ceb3d81` 首版 + `1f1d23c` 审查修复），当时为了不和未提交的 0.15.1 打架没动版本号；版本号与本条在这里补记。
> 同时做过一个「问答叙事」版（B），你比过两版后选了这一版；B 的两个提交留在标签 `archive/tokens-viz-b` 里备查。

**动机**：旧页每根条都按 token 量画，可 97% 的 token 是便宜的缓存读——token 条几乎就是缓存读条，钱花在哪反而看不出；
也没有「时段」「会话」两个维度。近两周的真实数据里有三件事旧页答不出：
缓存读占 97% 的 token、却只占 57% 的钱；44% 的钱花在凌晨 0–7 点；第一名的会话一个就占了 54%。

**交付**
- **数据立方** `GET /api/tokens/cube`（新模块 `tokmon/tokens_view.py`，在展示层，内核零改动）：按
  日期 × 小时 × 项目 × 模型 × 来源 × 会话分组，每组带各类 token、等价成本和成本拆分（输入 / 输出 / 缓存写 / 缓存读 / 工具），
  拆分与 `pricing.cost_usd` 逐项同式，各分项之和 == 成本；和 `/api/summary` 同一套窗口与基线口径，页面拿随包下发的总数自己对账。
- **一屏分析台**：6 枚 KPI（各带迷你趋势与 vs 上一周期）· 累计花费曲线（本期 vs 上一周期对齐；今天窗口的全天估算画虚线并写明方法；预算线）·
  项目矩形树图 · 成本构成（$ 与 token 两根 100% 条并排）· 按天 / 按小时堆叠柱 + 每百万 token 单价 · 星期 × 小时热力图 ·
  会话帕累托 · 变化最大的项目 · 模型占比 · 可折叠明细表。
- **联动筛选是主角**：点任何图形，其它图和 KPI 立刻在本地重算；同维度「或」、跨维度「且」；按天图可拖动框选；
  悬停联动；筛选状态全部进 URL；自动刷新保留筛选，数据没变不重画。
- **真实优先**：$ 处处叫「等价成本」（估算，不是账单）；未知单价用斜线纹 + * 标出，不悄悄消失；柱从 0 起、不平滑；
  零碎日子不拿来算单价倍数。零依赖、离线（手写内联 SVG），换上统一导航。
- 一轮三视角对抗式审查，确认 18 项全部修掉（高 2 · 中 7 · 低 9），每项有回归用例。

**验证**：立方与 `/api/summary` 在合成会话上逐项对账（5 个窗口 × 2 个口径）；真实数据五个窗口逐项对上；
Node 冒烟 + 真实浏览器 70 项交互检查全过、无控制台错误、无任何写请求。

## 0.15.1 — 会话状态以 Claude Code 自报为准（修 `/sessions` 误判）

**动机**：用户发现 `/sessions` 对「会话是否活跃」判断不准。2026-09-24 现场核对：本机 9 个活的 claude 进程，
页面上 5 个「运行中」里 **3 个是假的**，14 个「等你输入」里 **6 个早已关闭**，还有 1 个后台子代理在跑的会话显示「这轮已完成」。
根因是 transcript 只记「发生过什么」，看不见「现在卡在哪」；而 Claude Code 自己一直在
`~/.claude/sessions/<pid>.json` 里写着每个进程**承载哪个会话**、**这一轮是 忙 / 空闲 / 等你授权**。

**根因（逐条实测）**
- **进程重启后的残留回合**：VS Code 重启后各会话被 `--resume`，transcript 尾巴停在上个进程没跑完的工具调用 /
  没人回的后台通知。进程活着 → 旧逻辑把「久未返回」消歧成「运行中 · 长任务（进程在跑）」，挂了 3–9 小时。实际早已空闲在等你。
- **死会话冒充「等你」**：同一项目里只要还有别的会话开着，按 cwd 就证伪不了 → 活性「未知」→ 保留 transcript 的「等你输入」。
- **后台 Agent 没认出来**：它的启动回执是 `Async agent launched successfully.`（语料 16/16），
  不含识别用的 `launched in background` / `running in background`，于是一启动就被当成已结束。
- **只记不发的排队消息**：`queueTranscriptOnly: true` 的消息从不发给模型（语料 4 条，0 条被回复），却被当成最后一条消息 → 「处理中 / 久未返回」。
- **等你授权**在 transcript 里和长任务长得一样，旧逻辑只能并列「长任务 / 等授权 / 已关闭」。

**交付**
- **procmon 读会话注册表**（只读、只本机）：pid 必须活着，且进程出生不晚于登记时刻（防 PID 复用；崩溃遗留的陈旧登记自动丢弃）。
  已登记的进程只承载它登记的那个会话：同项目里所有活进程都登记了别的会话 → 这个会话**可证**已关闭。
  还有没登记的进程（刚启动 / 老版本）时照旧不下结论（P6）。注册表目录不存在 → 完全退回旧口径。
  也认 npm 装的 `node … claude-code` 进程，免得它们被漏数而让同项目会话被误判关闭。
- **activity 以进程自报为准**（`classify_state(proc=…)`，仍是纯函数；activity 仍不 import procmon）：
  `busy` → 运行中（前台这轮已说完 = 「后台运行中」，SDK 会等后台子代理跑完才报 idle）；
  `idle` → 等你（上一轮没收尾的写「已空闲（上一轮没有正常收尾）」）；
  `waiting` → **新状态 `BLOCKED_ON_USER`**：「等你授权 · 权限弹窗开着」/「等你回答 · Claude 在问你」，排在列表最前。
  两处保守：注册表还没跟上你刚发的话时，这一帧先信 transcript（不误发 TASK_COMPLETED）；
  本进程里启动、还没收口的后台 Workflow 照旧算在跑（它是否让 SDK 保持 busy 未证实，不回退旧 Bug1）；
  进程重启前启动的后台任务不再算「后台在跑」（它们随旧进程死了）。
- **transcript 侧**：跳过 `queueTranscriptOnly` 消息；认出 `Async agent launched` 回执（没写 flag 的 Agent 也凭回执认）；
  `/model` 这类本地命令的 `<local-command-stdout>` 判「等你」（以前是「处理中 → 久未返回」）。
- **`/sessions` 页**：「等你」计数含等授权，并单列「其中等你授权/回答」；徽章悬浮注明来源（进程自报 / transcript 推断）；
  页脚说明「久未返回」现在只在拿不到进程自报时出现。`BLOCKED_ON_USER` 同时接进 steer 忙碌保护、`/workflow` 运行态、风险 pump。
- **事件**：回合卡在你身上**持续 60 秒**（你不在键盘前）才提醒一次、每段等待一次：授权/沙箱 → `PERMISSION_NEEDED`，
  提问/对话框 → 新类型 `QUESTION_PENDING`（warning）。以前这种情形靠 2 分钟后的 SESSION_STUCK「可能…等授权」兜着，不能丢；
  现在是进程自报，不是推断。冷启动 / 首次见到时已经卡着的不补发。
  进程重启杀掉的上一轮转成「等你」时标 `turn_unfinished`：**不发 TASK_COMPLETED、不补发空闲台阶**（那不是完成）。
- **steer 更安全**：还开在某个进程里的会话（`liveness=True`）拒绝 resume（`session-open`），防两个进程写同一 transcript。

**评审**：一轮独立对抗式 review，确认并修复 7 项——
重载后误发「任务完成」· 等授权的提醒被静默（上一版自己的测试还把静默钉成了预期）· steer 能 resume 被 VS Code 持有的会话 ·
没写 flag 的后台 Agent 漏认 · 注册表说 idle 时后台 Agent 还被当成在跑（只有未证实的 Workflow 保留兜底）·
面板里刚换会话的一瞬被判已关闭（新增 30 秒宽限：刚写过消息的不判关闭）· 同一会话两个登记按目录顺序取（改为取状态最新的，
Windows 上再用 procStart 精确比对进程创建时刻）。另把「注册表滞后」的容忍从 120 秒收到 20 秒。
未处理（安全方向）：Claude Desktop 也叫 `Claude.exe`，若它开着，同目录会话只会退回「未知」，不会被误判关闭。

**验证**：38 个新单测（`tests/test_session_registry.py` 37 + `test_steer` 1）；两个旧测试的消息年龄挪出 30 秒宽限（意图不变）；
全量 437 passed。现场：9 个活进程 ↔ 9 行非关闭，状态与注册表逐一一致；**真机看到了 `BLOCKED_ON_USER`**——
一个会话停在 AskUserQuestion 上，页面显示「等你回答 · Claude 在问你」（旧逻辑会说「久未返回」或「长任务」）；
页面用 Edge 无头渲染无 JS 报错。**没验证到的**：真实的权限弹窗（`permission prompt`）还没在真机上碰到。

## 0.15.0 — 趋势 + 实时：在变好吗、现在有没有出事（改动与风险 S3）

**动机**：S1 知道改了什么，S2 知道有没有乱改。S3 回答剩下两个问题：**这一段比上一段好还是坏**；
**正在跑的会话现在有没有出事**——不用打开回放一个个翻。

**交付**
- **趋势（统计页新增「趋势」段）**：四组八个指标——
  质量（调用失败率 · 每 100 次调用里的重试 / 打转）、风险（命中风险规则的任务占比 · 改了没验证完的任务占比）、
  效率（每个任务 token 的中位数 · 活跃时长的中位数 · 偏慢调用占比）、外部依赖（MCP 失败率）。
  - 粒度跟着时间范围走：今天按小时 · 近 7 天按天 · 近 30 天与全部按周（周一起）。
  - 「vs 上一周期」和 `/tokens` 同一口径：今天比昨天 00:00 到同一时刻；其余比紧挨着的、同样长的上一段；
    全部时间没有上一周期，就直说没有。质量 / 风险 / 外部依赖降了是绿、升了是红；**效率只写涨跌、不评好坏**（任务大小不一）。
  - **样本太少的段不画成趋势**（调用 < 30 · 任务 < 3 · MCP 调用 < 5）：空心点、不连线、不参与比较，
    写「样本太少，不比」，不给一个会误导的百分比。上一周期的值画成虚线。
  - 趋势线上每一段都能点：列出那一段的任务，条数 == 那一段的任务数（和别的数字走同一套追溯索引）。
  - 排行行尾小趋势：工具（每段调用数）· Skill（每段用到它的任务数）· 风险规则（每段命中的任务数）·
    MCP（每段调用数，红 = 失败）。这些序列直接挂在各自那一行上，名字不单独出现，脱敏不会漏。
- **`/sessions` 角标 + 实时回放入口**：每个会话挂上「当前任务」的风险角标（悬浮看全部标记），
  「实时回放 →」直接打开 `/workflow` 里正在跑的那个任务（每 5 秒自动刷新）；已结束的是「回放 →」。一天没动静的老会话不算。
  `/api/sessions` 的快照是共享缓存（事件 pump 也在用），只拷贝后挂简报，绝不原地改。
- **风险事件进总线**：新增 `DESTRUCTIVE_OP` / `ERROR_SPIKE` / `REPEATED_FILE_EDIT` / `LARGE_DIFF`（每 15 秒看一遍开着的会话）。纪律：
  - **一律 `info`**：规则精确率审完之前不升级；notify 本来就不推 info——**通知仍然关着**；
  - 冷启动静默（已经在那儿的标记不当新事件）· 同一个任务的同一个标记只发一次；
  - 事件时间 = 这个标记最后一个证据发生的时刻，不是检测到的时刻（事件规范：`timestamp` 绝不是 now）；
  - payload 只有规则名 / 计数 / 破坏性操作的种类（如「rm -rf」），**不带路径、命令、文件名**（§6 allow-list 兜底）；
  - 「改到工作目录以外」只在页面上看，不进总线。
- **顺手打磨两项**
  - **MCP 失败原因归类**（代替「最近 3 条错误」）：同一种错归一类（网址 / 路径 / 编号 / 数字换成占位），
    每类的次数点开即明细，各类合计 == 失败数。真实数据：railway 的 11 次失败归成 3 类，
    其中 9 次是同一句「Unauthorized，请重新 `railway login`」——一眼看出是登录态过期，不是服务挂了。
  - **「偏慢」按命令族**：Bash / PowerShell 按命令族各建基线（`uv run pytest`、`git status`、`npm run build` 各算各的），
    阈值从 p90 提到 p95。整个 Bash 共用一个基线时，近 7 天标了 1,497 次偏慢（拿 pytest 和 ls 比，基本是噪音）；
    现在近 7 天是 3.7%。
- **UI**：全局导航把低频页（体检 / 回测 / 厂商账单）收进「更多」，七个页面共用一处定义（`_nav_html`）；统计页分段导航加「趋势」。
- 顺手修：`shell_parse` 的 docstring 里有 `\$`，每次重新编译都报一个 DeprecationWarning——之前测试里「偶发的 1 warning」就是它。

**完成判据**
- ① **趋势与同窗口汇总自洽**：四个时间范围在真实数据上逐项对账，全部相等——本期任务数 / 调用数 == 概览，
  各段相加 == 本期，失败率 == 失败 ÷ 调用，行尾小趋势各段相加 == 那一行的数，MCP 失败原因合计 == 失败数，
  最大的一段点开列出的条数 == 段上的数。单测里同样钉死（`tests/test_trends.py`）。
- ② **真实运行中的会话**：在这次开发会话本身做了一次无害的 `rm -rf`（删临时目录里刚建的空目录）。
  约 10 秒后 `/sessions` 上本会话出现「⚠ 破坏性操作：rm -rf」角标，总线上出现一条 `DESTRUCTIVE_OP`
  （info，payload 只有 `rule / count / kind`）；点「实时回放 →」打开的正是这个正在跑的任务，摘要里是同一条风险标记。
- ③ **统计接口实测耗时**（本机；全部历史 172 个任务；服务刚重启、基线热好之后）：

| 范围 | 第一次 | 缓存命中 | 重新统计 |
|---|---|---|---|
| 今天 | 0.22s | 2ms | 0.30s |
| 近 7 天（连上一周期 43 个任务一起算） | 1.46s | 42ms | 1.46s |
| 近 30 天 | 1.41s | 44ms | 1.43s |
| 全部 | 1.42s | 44ms | 1.42s |

  服务冷启动到耗时基线热好要 12.9s（要把全部 transcript 读一遍）。这段时间 `/sessions` 照常出（0.48s），只是先不挂角标；
  风险事件 pump 也在线程里等基线热好再播种——**第一版是同步播种，会把服务启动卡住 16 秒，自测时发现后改掉**。
  正在跑的大会话每秒都在变，它的简报 5 秒内复用一份（大会话算一次约 0.5s）。

**真实数据一瞥（近 7 天 vs 上一个 7 天）**：调用失败率 2.6%（↓1.0 个百分点）· 命中风险规则的任务 22%（↑0.7）·
改了没验证完的任务 75%（↓6.5）· MCP 失败率 26%（↓3.0）· 偏慢 3.7%（↓9.4：上一周的偏慢集中在一个项目的 Write，
中位 4.4s，单独复算确认数据如此）· 每个任务 token 的中位数 5.4M（↑513%：这周多是长会话，任务构成变了——
这就是效率组只写涨跌、不评好坏的原因）。

**没做 / 债**
- 风险规则仍全是「提示」，事件仍全是 info——升级要等精确率审完。
- Token 看板（`/tokens`）还是老页头，没有全局导航，这次没动。
- 统计第一次 1.4s 基本花在构建任务上；冷启动缓存仍在债里。

测试 384 → 406（`python -m pytest -q`）。

## 0.14.0 — 风险标记：有没有乱改、有没有打转（改动与风险 S2）

**动机**：北极星四象限里最弱的一格是「乱改」。S1 已经知道改了什么，S2 在它之上加三类规则，
**只标不拦**，而且每条都要说得出「凭什么这么判」。

**交付（三类规则，全部是推断）**
- **破坏性操作**：`rm -rf`（递归和强制要同时有，`rm a.txt` 不算）· `git reset --hard` · `git clean -f` ·
  强推（`--force`；`--force-with-lease` 不算）· 整棵树还原（`git checkout/restore .`）· PowerShell 递归强删 ·
  MCP 的 remove / delete 类调用 · 数据库客户端里的 `DROP` / `TRUNCATE`。**预演不算**（`git clean -n`、`--dry-run`、`-WhatIf` 一个字节都不删）。
  命中的那次调用在回放里直接挂徽章。
- **打转类**：`改这个文件 → 验证失败` 的来回 ≥ 3 轮——**按时间线分开算**（workflow 里几个 agent 各自在自己的 worktree
  改同名文件，不是一个人在来回），而且**失败输出里点了这个文件的名才算它的一轮**（整条时间线的失败不摊给每个被改的文件）；
  同一条时间线上**严格相邻**地连着失败 ≥ 3 次（中间成功过就断）。
- **大改动 / 越界**：单次调用增删 ≥ 1200 行 · 一个任务动 ≥ 200 个文件 · 改到工作目录以外。
  越界里**不算**的：Claude 自己的临时脚本、自动记忆、系统临时目录（路径照写，只是不算越界）；**算**的：别的仓库、
  `~/.claude` 下的设置 / agent / skill（改它们会改变 Claude 的行为）。
- 显示：回放摘要的「风险」块（有对应调用的点一下跳到第一处，按任务判的不假装能点；悬浮看规则原文）· 调用行徽章 ·
  任务列表角标 · 统计页「风险」小节（任务数与相关调用数都点开即明细）。统计页分段导航加了「风险」。
- **分级只用了「提示」**：没有一条升到「警示」——按纪律，升级要有实测精确率支撑，这一轮观察量还不够（强推近 7 天一次没出现）。

**真实数据校准（近 7 天 104 个任务，目标「少而准」）**
| 规则 | 命中任务 |
|---|---|
| 破坏性操作 | 16.3% |
| 连续失败（严格相邻 ≥ 3） | 4.8% |
| 打转（改了又改、失败点名） | 1.9% |
| 单次大改动 | 4.8% |
| 一个任务动很多文件 | 5.8% |
| 改到工作目录以外 | 3.8%（剩下的都是真该看的：改了 `~/.claude/settings.json`、动了 transcript 备份） |

抽样逐条看了命令原文：破坏性、连续失败、大改动都确有其事。「破坏性」不等于「做错了」——在临时目录 `rm -rf`、
在一次性 worktree 里 `git checkout -- .` 都是有意为之，所以它只是提示，界面上也这么写。

**多 agent 对抗式 review（规格判据④）**：4 个维度（规则误报 / 命令解析 / 追溯一致性 / 页面与脱敏）各 1 个 reviewer，
每条发现 1 个专门去推翻它的复核者，共 30 个 agent、约 29 分钟、230 万 token。**26 条发现，24 条成立（去重后约 20 个问题），全部修掉**，
每条都变成了回归用例；2 条被推翻（分段导航其实好用、`why` 字段是固定文案不需要脱敏）。成立的里面分量最重的：
- **命令解析的根因**：之前是几个各自为政的正则和扫描器，于是 `"$SP"/a.py` 被拆出一个根目录下的假文件 `/a.py` 并标成「越界」、
  引号里的 `|` 被当管道（`sed -i "s|a|b|" f` 造出一个叫 `s` 的文件）、`# 注释` 被当参数（`rm -rf dist # remove stale build` 多出 3 个被删文件）、
  heredoc 之后的命令被整段丢掉（全库漏记 365 次写文件、37 次破坏性操作）、引号里的 `<<` 被当 heredoc、flag 的值被当路径
  （`-ItemType Directory` 造出文件「Directory」）。**改成一个统一的 shell 词法器**：引号与转义、注释、控制符、重定向、
  heredoc（正文跳过、终止符之后接着解析、没闭合记「说不清」）、同一条命令里字面量变量的展开（`SP="C:/x"; rm -rf "$SP"/y` 认得出）。
  改动清单、阶段判定、破坏性判定全部走它。
- **打转把整条时间线的失败摊给每个文件**（只改文档也会被判「改了 3 轮没过」）→ 失败要点名。
- **`git clean -nd` 这类预演被当破坏性** → 按词判，预演一律不算。
- **口径**：「处」有两种算法、文件行「N 次」与展开条数对不上、界外数可能大于总文件数、`~/.claude` 下不同文件被合并成一个「临时脚本」、
  只有「说不清」改动的任务显示「0 个文件 · 0 处」、Git Bash 写法的 `/c/Users/...` 没被认成 `C:/Users/...` → 全部统一到「调用」口径并修正。

**修完之后**：git 对账覆盖率 80.4% → **85.1%**（heredoc 之后的命令、变量路径找回了真改动；按仓库 项目 A 85.3% · 项目 B 87.4% ·
项目 C 70.6% · 本仓库 57.7%）。统计页所有改动与风险数字与明细条数 **0 处不一致**。

**验证**：`pytest -q` **384 passed**（318 → 384；新增 `tests/test_risk.py`，review 的每条复现都是回归用例）。
页面脚手架在 8 个真实任务上跑 169 次明细抽屉、363 个统计数字、117 次跳转，**0 错误**（顺带修了脚手架自己的取快照顺序：
另一个正在跑的会话在「导出回放」和「算统计」之间长出新调用，会让跳转目标暂时不在导出的树里）。

## 0.13.0 — 改动清单：这次任务到底改了什么（改动与风险 S1）

**动机**：回放能告诉你「它是怎么做的」，但一眼看不出**「它到底改了哪些文件、改完验证了没」**。
这也是北极星四象限里最弱的一格「乱改」的地基。细节见 [CHANGE_RISK_PLAN.md](CHANGE_RISK_PLAN.md)（五轮问卷，19 项决定）。

**交付**
- **回放摘要里的「改动」块**：N 个文件 · M 处 · +X/−Y 行 · 验证状态；文件表给出每个文件的改动次数、动作
  （改 / 写 / 删 / 移 / 还原）、增删行数、来源标签；点一个文件展开它的每一次改动，点一条跳到回放里那一步；
  可切「改动时间线」按时间顺序看先改哪后改哪。
- **两级来源，界面上分开标**：`Edit` / `Write` / `NotebookEdit` 是**真值**；`Bash` / `PowerShell` 是**推断**
  （认 `sed -i`、重定向、`mv` / `cp` / `rm` / `touch` / `tee`、`git checkout -- 路径` 等）。
  看得出在改、却认不出改了哪个文件的（内联脚本、`git reset --hard`、通配符、解压）**如实记成「说不清」**，绝不瞎猜文件名；
  `git add` / `git commit` / `mkdir` / `chmod` 不动文件内容，不算改动。
- **行数用实际落盘的补丁**：工具返回里带 `structuredPatch` 时按它的 +/− 行算（真值，实测约 29% 的 Edit/Write 有），
  否则退回按请求的新旧文本算；`replace_all` 替换了几处不知道 -> 标 `?` 说明是下限；Write 覆盖前多大不知道也标 `?`。
- **「改完验证了没」说得更准**：给的是「最后一次验证是什么（成功 / 失败）+ 它之后又改了多少处 / 多少文件」，
  而不是一句「没验证」——跑完测试又补个文档，和改完根本没跑过测试，不是一回事。
- **左栏按文件反查**：搜索框里输文件名或路径片段，列出改过它的任务，行里标出改了几次。
- **统计页新增「改动」小节**（数字同样点开即明细）：有改动的任务 / 改了文件的调用 / 涉及文件数 / 增删行数，
  推断的改动、认不出文件的改动、界外文件，以及「改完验证过 / 验证后又改了 / 全程没验证」三个口径。
  统计页同时加了**顶部分段导航**（概览 / 改动 / 工具 / Skill / MCP / 对照）。
- 路径按任务的工作目录归一；Claude 自己的临时脚本目录不算「界外」，显示成 `临时脚本/xxx`。

**拿 git 对账（完成判据，如实报）**
- **仓库汇总口径 81.2%**：近 30 天窗口里，提交碰到的 2,607 个文件中有 2,116 个出现在某个任务的清单里。
  按仓库：项目 A 85.2% · 项目 B 78.3% · 项目 C 56.9% ·
  **claude-token-monitor 只有 55.7%** —— 因为我在这个仓库里经常用一次性 python 脚本改文件，那正好落在「认不出文件」那一类。
- **严格按任务口径只有 37.4%**，但这个数字不该直接读成「漏了 63%」：一次提交会把**之前任务**积累的改动一起带上。
  实测有任务一次提交 226 个文件，其中没有一个是它自己改的。
- 已知补法（这次没做，记在债里）：transcript 里的 `edited_text_file` 附件会记下「工具之外改过的文件」（全库 293 条，
  只在文件已在上下文里时才发），能补上一部分脚本改动。

**验证**：`pytest -q` **318 passed**（282 → 318；新增 `tests/test_changes.py`：命令解析逐条、行数口径、验证口径、
路径归一、清单组装、统计数字与明细一致、脱敏）。真实数据近 7 天 100 个任务里 84 个有改动：5,982 个文件、8,983 次改文件的调用，
其中 1,932 次认不出文件；改完验证过 16 个任务、验证后又改了 43 个、全程没验证 25 个。
页面脚手架在 9 个真实任务上跑 173 次明细抽屉 + 350 个统计数字 + 142 次跳转，**0 错误**；改动块的展开、时间线、跳转都在测试里。
列表 100 个任务 0.7–1.0 秒、统计约 1.2 秒（改动清单不额外扫盘）。按约定 S1 自查 + 单测，多 agent review 放在风险规则那一刀。

## 0.12.0 — `/workflow` S3 统计子页：工具 / skill 排行 · MCP 健康 · 跨任务对照（每个数字都能追溯）

**动机**：S1 让你追踪一次任务，S2 让你看懂它；S3 回答「横着看」的问题：哪些工具 / skill 最花时间和 token、哪个 MCP 老出错、
同一个阶段 / 角色在不同任务里差多少。完成判据是规格里那条：**排行数字与回放逐项可追溯**。细节用 1 轮问卷敲定（4 项决定）。

**交付**
- 页面顶部多了「回放 | 统计」切换（`/workflow?view=stats` 可直达）。统计跟随左栏的时间范围和项目；搜索框只筛列表。不做趋势，只看所选范围的汇总。
- **概览**：任务数 / 调用数 / 活跃时长合计 / token 真值合计（带等价 $）/ 调用失败率；重试 · 打转 · 超大结果 · 偏慢四个标记的总数。
- **工具排行**：调用 / 任务 / 耗时合计 / 中位与 p90 / 失败率 / 其他标记 / 结果体积 ≈。默认按耗时排，点表头换排序；MCP 按 server 合成一行。
  AskUserQuestion / ExitPlanMode 是**等你**的时间（近 7 天 30 次共 39 小时），单列一行，不参加排行；Agent / Workflow 行标「含子 agent」（嵌套，不能和别的行相加）。
- **Skill 排行**：一个任务里用到算一次（被打断分成几段的合在一起）；token 是归属到它的真值；没有归属标记的那几次，时长和 token 都「拿不到」，合计标 ≥。
- **MCP 健康**：每个 server 的调用、失败率、中位 / p90，逐工具的调用与失败，最近 3 条错误原文（已脱敏，点一条跳回放）。
- **跨任务对照**：Skill / Workflow 阶段 / Agent 角色 / 工具四个维度，指标可切 耗时 / token / 调用数。每个点是一次（对数轴，同一维的行共用刻度），
  竖线是中位；右侧给出 中位 · 最慢 · 最贵，点任意一个点或「最慢 / 最贵」都跳到回放里的那一次。
  角色 = 标签冒号前的前缀（review:auth → review，按命名约定**推断**）；没有前缀的按 agent 类型（真值）。
- **追溯**：每个带虚线的数字都能点开，抽屉里列出的条数**就是**这个数字（可按时间 / 耗时 / token 排，100 条一页）；
  点一条 → 切到回放、打开那个任务、跳到那一步并打开它的明细。页面拿到的每份统计带一个版本号，取明细时用的是算出这些数字的**同一份**；
  那份过期了，就按同样的窗口重算，并同步刷新页面上的数字——数字和明细不会对不上。
- 接口：`/api/workflow/stats` · `/api/workflow/drill`（同样过读门、同样脱敏；$ 由 serve 层注入换算，trace 支柱仍不依赖计价）。

**过程中发现并修掉的**
- 「等你」混进了工具排行：按耗时排，AskUserQuestion（39 小时）压在第一位 → 单列。
- 没有归属标记的 skill 段被算成 0 秒：它在回放里只装着加载它的那一步，真实时长不知道 → 改为「拿不到」，不写 0。
- 同名 skill 嵌套（子 agent 的行也带着父 skill 的标记）会把子 agent 的调用和 token 算两遍 → 调用按 id 去重，时长 / token 只累加最外层。
- 统计缓存的键没带数据目录，不同目录的同一窗口会串结果（测试里暴露）→ 已修；统计版本只留最近 4 份（全部时间一份约 10MB），登记表跟着清。
- 页面样式撞名：MCP 卡片的 `.mcp` 同时套到了 `mcp·xxx` 标签上 → 改名。

**验证**：`pytest -q` **282 passed**（262 → 282；新增 `tests/test_trace_s3.py`：聚合、嵌套去重、未标注 skill、「每个数字 == 明细条数」、
明细指向真实节点、缓存 / 过期 / 脱敏）。真实数据**全部时间** 146 个任务 / 19,482 次调用：所有可点数字与明细条数 **0 处不一致**。
页面脚手架在近 7 天 100 个真实任务上点遍 323 个可点数字（条数全部相等），做了 353 次跳转（全部落到回放里的那一步），0 个错误。
无头 Chrome 实测：点 MCP 失败数 13 → 抽屉列出 13 条 → 点第一条 → 回放打开那个任务和那次调用的明细。
统计冷启动（服务刚起、预热中）约 20 秒，之后重算 2–4 秒，30 秒内复用。按约定 S3 自查 + 单测，不跑多 agent review。

## 0.11.0 — `/workflow` S2 学习层：流程条 / 数据依赖 / 名词说明 / 脚本对照

**动机**：S1 让你能「追踪」一次任务，S2 要让你能「看懂、学到」它是怎么完成的。细节用 2 轮问卷敲定（8 项决定）。

**交付**
- **阶段标注（推断）**：每个调用按规则归进 查看 / 修改 / 验证 / 运行 / 调研 / 编排 / 等你；连续同类合成一段，
  夹在两段同类之间的单次碎片并进去（失败与等你永远不并）。摘要里一条可点的流程条，时间轴顶部一条同色色带；
  主线程与每个子 agent 各算各的。Bash 看命令本身（去掉 `cd` / 变量赋值前缀，`&&` 链里有测试就算验证，写文件算修改），
  看不出目的的归「运行」。真实数据：查看 49% · 修改 28% · 运行 11% · 验证 9%。
- **MCP 数据依赖（推断）**：MCP 调用参数里像 ID 的值（UUID / 长十六进制 / 带数字的长串）若在此前某次调用的返回里出现过，
  就标一个「⤷ 用了 X 的结果」徽章；悬浮高亮来源行，点击跳过去；明细里写明是哪个字段、哪个值对上的。来源可以是任何调用
  （Bash 读配置拿到的 ID 也算），目标只看 MCP。真实数据：64 次 MCP 调用里找到 19 次来源。字段名像密钥的不给值。
- **名词说明**：35 种内置工具由我写中文简介；skill、子 agent 类型、MCP server 用 transcript 里的官方原文（只下发任务里用到的）。
  悬浮在工具名 / skill / agent 上看，明细里有「这是什么」。MCP 单个工具的说明 transcript 里没有，如实说明，不编。
- **脚本对照**：workflow / 阶段 / agent 的明细里嵌入编排脚本，阶段行紫底、派 agent 的行蓝底，并跳到对应行。
  阶段认三种写法（`phase()` 调用 / `agent()` 参数里的 `phase:` / 头部 meta 声明），agent 标签认字面量、模板、拼接三种。
  **真实数据 43/43 次 workflow 的阶段全部对上脚本**；agent 对上启动它的那一行 370/391。
- **页面冒烟测试进了测试套件**：`tests/js/workflow_smoke.js` + `tests/test_workflow_page.py`，用合成会话跑遍渲染、展开、
  每类明细、跳转与 S2 的六个新部件，异步加载里被吞掉的错误也会报出来（RECAP 侧批 #6：前端此前零覆盖）。

**过程中发现并修掉的**
- 脚本找不到（第一版只有 21/43 对得上）：workflow 还有「`agent()` 参数里写 `phase:`」的写法，比 `phase()` 更常见；
  另外 Claude Code 按**启动 workflow 那一刻的工作目录**选项目目录存脚本（会话中途 `cd` 进子目录、或在 worktree 里启动时，
  脚本不在会话自己的目录下）——现在优先读 Workflow 返回里的 `scriptPath` / `transcriptDir` 真值（限定在 `~/.claude/projects` 之内）。
- 列表变慢（0.28 → 0.83 秒）：每个任务都重扫整个会话的 agent 文件找「被明确引用的 agentId」，改成会话级缓存后 0.47 秒。

**验证**：`pytest -q` **262 passed**（223 → 262）；真实数据 98/98 个任务 token 与 parser 逐 token 一致；
页面脚手架在 13 个真实任务上跑 247 次明细抽屉，0 个错误。按约定 S2 自查 + 单测，不跑多 agent review。

## 0.10.0 — `/workflow` 工作流回放 S1（+ 一轮多 agent 对抗式 review）

**动机**：你要一个比 /tokens 更细的追踪器——看一次提问是**怎么被完成的**：调了哪些 skill / 工具 / MCP /
子 agent / workflow，各花多少时间和 token。原则是你的原话：「能追踪、能看懂、能学到；不缺信息，也不堆信息」。
需求用 6 轮问卷敲定（23 项决定），规格见 [WORKFLOW_TAB_PLAN.md](WORKFLOW_TAB_PLAN.md)。远程控制与通知按你的决定搁置。

**交付**
- 新支柱 `tokmon/trace.py`（只读，只 import stdlib + discovery + project）：按真人提问切任务；后台任务完成通知按
  `<tool-use-id>` 真值找回启动它的任务；Agent / Workflow 按 `agentId` / `runId` 回链子树，workflow 阶段顺序取自脚本；
  skill 归属只信 Claude Code 自己打的标记。按文件增量解析 + 缓存，近 7 天列表 0.3 秒。
- 新页面 `/workflow`（独立文件 `tokmon/pages/workflow.html`）：左栏任务列表；结构化摘要（耗时拆成 机器执行 / 模型生成 / 等你、
  token 真值与等价 $、异常计数、关键时刻）；调用树与时间轴并排；明细抽屉取完整输入输出与全文（已脱敏）。
- 三级诚实：真值直接写；调用级 token 是两个估算（发起 ≈ / 结果 ≈）；重试、打转、偏慢写明「推断 / 统计」。
- 导航隐藏 /notify /control（代码保留）；/tokens 的项目名可深链进来（带时间窗）。

**对抗式 review**：4 个维度各 1 个 reviewer + 1 个 verifier（8 个 agent）。34 条发现，**31 条确认，全部修复**，每条都有回归测试：
- **时间**：旧算法把一段里首尾之间整段算成「模型生成」，空闲、事后敲的 `/model`、恢复会话时的占位消息都会撑大时长——
  真实数据里一个 18 分钟的任务被报成 40 小时。现在只有相邻活动相隔 ≤ 15 分钟才连成区间；`/model` 归下一轮。
- **对账**：按指令开头兜底回链 agent 可能挂错、漏算或跨任务重复算 → 改为证据制（指令全文 + 启动时间唯一匹配，
  明确被引用过的 agent 不许碰，多个候选就不猜）。
- **运行态**：运行中给到「拥有最后一条活动」的任务；查询带进程存活索引——**修掉了 S1 对 /sessions 的一处回归**
  （activity 的共享快照缓存曾被写入一帧不带存活信息的结果）。
- **不把缺失写成 0**：文件缺失 / 没结果 / 含估不出的块一律显示 — 或 ≈?，合计有缺失时标「≥」。
- **结果体积**：图片按尺寸估（原来「[图片]」3 个字 = 3 token）；skill 正文注入算进 Skill 调用；另存到文件的输出不再误报「大结果」。
- **安全**：transcript 里出现过的控制令牌会原样出现在回放页（本仓库测远程模式时打印过）→ 本服务知道的密钥原值精确打码；
  截断不再切出半截密钥；按字段名打码嵌套 JSON 里的 password / apiKey 等。
- **其余**：后台任务失败不再显示成成功；「打转」要求输出也没变化（改代码→重跑测试不再误报）；「偏慢」写明是统计推断并排除
  sleep / 轮询；抽屉晚到的旧响应不再覆盖新内容；自动刷新失败不再清空回放；吸顶表头修复；启动横幅恢复外发提示。

**验证**：`pytest -q` **223 passed**（177 → 223）；真实数据 **97/97 个任务、20/20 个会话 token 与 parser 逐 token 一致**
（仅有的不一致都复核为活会话写入竞态，换序后一分不差）；被报成 40 小时的任务现在是 18 分 09 秒；
139 个任务的回放数据与 69 个调用明细里控制令牌出现 0 次（原始 transcript 里有 2 次）；前端在 Node 桩 DOM 上用 12 个真实任务
跑 213 次明细抽屉，0 个运行时错误。

## 0.9.0 — 重启四件事：停止撒谎的默认值 / 补上读页鉴权 / 备好手机环 / 文档降熵

**动机**：2026-09-20 的全景评估（[docs/RECAP_2026-09-20.md](docs/RECAP_2026-09-20.md)）指出，
这个系统「机器造得很干净、信任基建做到可测量的 97%，但最有价值的那一下从未通过电」，
且有三处与它自己的原则相悖。本版按「杠杆/成本」而非原路线图，把其中四件事做掉。

**① `/sessions` 不再默认藏起「谁在等我」**（原则 1 真实优先 / P6 误报零容忍）
- 「隐藏空闲会话」**默认不再勾选**。实测当天数据：10 个「等你」里 9 个会被旧默认删掉
  （V3 审计当时是 71 分之 70）——这一页唯一的存在理由被它藏掉了。
- 「空闲」这个**语义反了的词**全站改名为「**等你已久**」：它的真实含义是
  「Claude 已经回复你、超过 10min 没人接话」= **你欠它一句话**，不是「它闲着」。
  pill 上也标明它是「等你」的**子集**而非并列状态。
- 代码里写下了「未来若想改回 `checked`，先回答『用户打开这一页是为了看什么』」。

**② `MC_REMOTE`：读页鉴权收口**（[REMOTE_CONTROL_PLAN.md](REMOTE_CONTROL_PLAN.md) §6，欠了两个月）
- 新增 `tokmon/remote.py`（只 import stdlib，纯函数为主）。开 `MC_REMOTE=1` 后
  **所有页面与 `/api/*` 都要令牌**；手机经 `/login` 贴一次，存 **HttpOnly + SameSite=Strict** Cookie（12h）。
- **两道门故意不合并**：读门认 Cookie（浏览器导航没别的办法），
  **控制门只认 `X-Control-Token` 头、永不认 Cookie** —— 合并就等于把控制面送给 CSRF。
  有一条真 HTTP 用例钉死这条不变量。
- **失败安全**：绑非本机却没开远程 / 开了远程却没 `MC_REMOTE_HOSTS` / 没令牌 → **拒绝启动**。
- Host 白名单是**显式登记**（不支持通配），保留 DNS-rebinding 防护；顺带修掉
  `normalize_host` 对无端口 `[::1]` 的误拒。
- hook 令牌改走 **header**（隧道边缘会把 query 写进日志）；本机 URL 保留 query 兼容旧配置，
  经隧道来的请求一律只认 header。

**③ 手机环：Pushover 出站通道**（M4.5(b)，平台唯一的价值出口）
- `notify.py` 的通道升级为**可插拔多通道**：Telegram（现只出）+ **Pushover（新）**，
  **全部默认关**，都不配就一个字节不出本机；单个通道挂掉不拖垮另一个。
- `pushover_priority`：critical → high（突破手机端免打扰），其余 → normal；
  **永不使用 emergency(2)**——那会重试到你手动 ack，正是 §7「有用且不烦」要避免的。
- `/notify` 页给出 Pushover 的最短上手路径 + 双通道状态。
- **诚实边界**：真账号才能验「手机真的震了」；但**发出去那个 POST 长什么样**已用
  本地 stub 服务器零外发地钉死（token/user/message/priority/title 逐字段断言）。

**④ 文档降熵 + 两条「开箱裂缝」**
- README **重写**：一页说清现状，并成为**唯一的文档索引**（每份计划文档标注真实状态）。
- 每份计划文档头部盖「真实状态」章：已实施 / 部分 / **从未实施**。
- `requirements.txt` 补 `pytest`（此前照着装会以为「这项目没测试」）。
- **修掉 GBK 控制台下 `doctor` / `backtest` 直接崩溃**（`UnicodeEncodeError: '✓'`，
  EVOLUTION §Gen0.3 记录在案、违反原则 5）：新增 `util.term_text` 做编码降级，
  写不出就换 `[OK]/[!]/[X]`，**绝不让展示层的编码问题杀掉已经算对的结果**。
- `__version__` 从停滞的 `0.3.0` 更正为 `0.9.0`（不是 1.0：手机环真机验证仍未完成）。

**验证**：`pytest -q` **177 passed**（130 → 177，新增 47 例：`MC_REMOTE` 29 + Pushover 15 + 编码降级 3）；
`MC_REMOTE` 两条拒绝启动路径与完整登录链路经真实 HTTP 实测；
GBK 控制台下 `doctor` 从崩溃变为正常跑完。

## 0.4.0 – 0.8.x — 未记录期（2026-06-28 → 07-20）

这段时间交付了 **serve 驾驶舱 / M1 对话活动 / M2 事件总线 / M3 通知层 / M3.5 预算告警 /
M4 控制层（远程审批）/ M4.5 推断 doctor + 准确率回测 / B1 厂商账单 / S1-v2 SDK steer**，
但 CHANGELOG 全部漏记。**不补写细节**（无法诚实重建），只给索引：

- 逐代评估 + 每代的证据分级 → [EVOLUTION.md](EVOLUTION.md)
- 各切片的设计与约束 → 见 README 的文档地图
- 当前全景与缺口 → [docs/RECAP_2026-09-20.md](docs/RECAP_2026-09-20.md)

## 0.3.0 — `tokmon doctor` 数据契约体检

**动机**：tokmon 依赖 Claude Code 未公开的 JSONL 内部格式（北极星 §5 标记的最大系统性风险）。
需要一个工具在格式漂移**悄悄算错前**把问题暴露出来，并让人**敢信**这些数字。

**变更**：
- 新增 `tokmon/doctor.py` + `tokmon doctor` 命令。全量扫描后体检：
  - **识别率**：assistant 消息 / 含 usage / 可解析时间戳 的比例。
  - **关键字段覆盖**：cwd（项目归属）/ message.id+requestId（去重）/ cache_creation 拆分。
  - **去重健康**：空去重键风险；**跨来源碰撞**（main 与 subagent/workflow 是否共用同一去重键）。
  - **未知模型**：不在定价表里的 model，按 token 量排序（提示去 `pricing.py` 补价）。
  - 带状态图标（✓/⚠/✗）与结论，对高去重率等给出解释而非裸数字。
- 新增 `test_doctor_scan_counts_and_warns`（共 17 例）。

**用 doctor 实测到的结论**（真实数据）：
- ✓ 数据契约健康：assistant/usage/timestamp/cwd/message.id/cache 拆分覆盖 ≈100%。
- **0 跨来源碰撞** → 退役北极星技术债 #3（来源拆分歧义经验证不存在）。
- 73% 去重合并率属正常：会话续接/分叉会重复记录历史（一条消息可出现 20 次/跨 5 个文件），
  去重是必要的，否则会多算约 3.7 倍。

## 0.2.0 — 项目身份对齐到 `.vscode` 子文件夹

**动机**：核心目标明确为「监控 `.vscode` 下每个子文件夹的 token 用量」。
v0 用编码目录名 `split('--')[-1]` 猜项目名，有两处硬伤：把空格变成 `-`、无法把更深的 cwd 归并回所属项目。

**变更**：
- 新增 `tokmon/project.py`：纯函数 `workspace_identity(cwd)`，从真实 `cwd` 还原项目身份。
  - 项目 = `.vscode` 下的**直接子文件夹**（大小写不敏感匹配 `.vscode`）。
  - 更深 cwd 归并回所属子文件夹，余下路径存 `subpath`（备未来 drill-down）。
  - 空格等原样保留；不在 `.vscode` 下的会话回退到 basename。
- `UsageRecord` 新增 `cwd / subpath / under_vscode` 字段。
- `parser` 改为单次扫描捕获文件级 `cwd`，按真实路径定项目，目录名仅作 fallback。
- 新增 `--vscode-only` 过滤（report / watch）。
- watch 的「正在跑」改用最新一条用量记录推断（移除对文件 mtime 的依赖）。
- 新增 `tests/test_tokmon.py`（13 例，无依赖可跑）：project / pricing / parser 去重。

**对抗式 review 修复**（多智能体 review 发现, 均补了单测）：
- **逐记录 cwd 归属**（HIGH）：之前整文件用第一个 cwd, 会话中途 `cd` 会把 token 算错项目。
  改为按每条 assistant 消息自己的 cwd 归属。实测修正了 home 会话约 33.7M tokens 的项目归属、
  ~26.8M 的 subpath 归属。
- **嵌套 `.vscode` 取最近一个**（MEDIUM）：避免外层 `.vscode` 漏进 subpath。
- **盘符/驱动器相对路径**（LOW）：`_split_path` 剥离 `c:` / `c:rest` 前缀。

**验证**：16 个单测全过；`--vscode-only` 现干净显示 4 个真正的 .vscode 子文件夹
（`API` / `edgar api` / `evolution-test` / `news feed 0622`）；home 开发会话正确拆为
`projects` / `claude-token-monitor` / `zwdzw`。

## 0.1.0 — 雏形

- 数据来源：`~/.claude/projects/**/*.jsonl`（只读）。
- main/subagent/workflow 三层来源分类；按 `(message_id, request_id)` 去重。
- 按模型家族定价 + 缓存 5m/1h/读 倍率。
- 两种形态：`watch`（实时 TUI）+ `report`（按需报表）。
