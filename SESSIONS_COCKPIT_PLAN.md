# 会话驾驶舱 · 一页规格

> **【状态章 · 2026-09-25】规划已批（「全部按你的推荐来」）· S0 进行中。**
>
> 来源：2026-09-24 的下一版规划 + 2026-09-25 你的批准。刻意只写一页：决策 + 边界 + 判据。
> 实现细节在代码与单测里（沿用 [WORKFLOW_TAB_PLAN.md](WORKFLOW_TAB_PLAN.md) / [CHANGE_RISK_PLAN.md](CHANGE_RISK_PLAN.md) 的纪律）。

## 0. 为什么是它

北极星要随时回答四件事：**推进 / 卡住 / 烧钱 / 乱改**（[MISSION_CONTROL.md](MISSION_CONTROL.md)）。四件事现在各有页面在答，
但你最常用的 `/sessions`（0.15.1 就是因为你发现它误判才修的）只答了三件半：

- 状态：0.15.1 起是进程自报，不再靠猜；风险：0.15.0 起每行有当前任务的风险角标。
- **烧钱：一个数都没有。** 要看钱得切到 `/tokens` 或点进回放。
- **卡住等你：只有你盯着页面时才看得到。** 开着一排会话时，一个停在授权弹窗上，可能十几分钟后才被发现。

所以这一轨不加新页面，把已有的几块拼成一屏，再让「等你」这件事能主动找到你。

## 1. 三刀

| 切片 | 内容 | 完成判据 |
|---|---|---|
| **S0 搬家**（纯重构，不升版本） | `serve.py` 里内联的页面搬到 `tokmon/pages/*.html`（`/workflow`、`/tokens` 早就是这样） | ① 每个页面搬家前后**逐字节一致**；② 行为零变化，全量测试通过 |
| **S1 一行看全**（0.17.0） | 每行加：当前任务的 token 真值 + 等价 $ · 近 10 分钟新烧 · 活跃时长 · 改了几个文件；页顶一条汇总：等你 N（其中等授权 / 等回答）· 运行中 N · 今天已烧 · 有风险的会话 N；修「重启后约 13 秒没角标」的空窗 | ① 每行 token 与同一任务回放页头的数**逐个对账全等**；② 在真实会话上截图核对；③ 页面刷新耗时写进 CHANGELOG |
| **S2 等你时叫你**（0.18.0） | 某个会话卡在你身上 ≥ 60 秒（授权弹窗 / Claude 在问你）→ 开着的 Mission Control 标签页弹浏览器通知，标签标题变「(N) 等你」；点通知回到页面并定位到那一行 | ① 真机上让一个会话停在提问上，约 60 秒弹出、同一段等待不重复；② 真的碰一次权限弹窗也会响（补 0.15.1 没验证到的一环）；③ 用一周，把「响了几次、几次有用」写进 CHANGELOG（M4.5 L4 打扰预算的轻量版） |

## 2. 决策

| 维度 | 决定 | 备注 |
|---|---|---|
| 放在哪 | **融进 `/sessions`**，不加新页面 | 页顶汇总条 + 行内字段 |
| 烧钱口径 | 当前任务的 **token 真值** + **等价 $** | 与 `/workflow` 回放页头同一个数；$ 与 `/tokens` 同一套单价，写「等价」（估算，不是账单） |
| 「正在烧」 | 近 10 分钟新增的 token | 按 API 响应时刻落在窗口内的 usage 相加 |
| 通知范围 | **只对进程自报的「等你授权 / 等你回答」** 持续 ≥ 60 秒 | 这是事实，不是推断；风险事件继续静默 |
| 通知通道 | **浏览器通知（本机，零外发）** | 不要账号、不碰远程控制；默认关，开关存在浏览器本地；每段等待只响一次 |
| 与 09-21 决定的关系 | **部分推翻「通知搁置」**：只开本机这一条 | 手机推送（Pushover）与远程控制仍搁置 |
| review | S1 / S2 自查 + 单测 | S2 用一周实测代替多 agent review |
| 隐私 | 仓库是公开的 | CHANGELOG / 规格里的真实数据一律化名（项目 A / B / C），不写真实花费；截图不提交 |

## 3. 口径与诚实边界

- token 是 transcript 里 API usage 去重后的**真值**；拿不到归属标记的段落照旧标「≥」（下限）。
- 浏览器通知**只能把你带回这个页面的那一行**，带不回 VS Code 里那个窗口。
- **页面没开就不会叫**——这是本机方案的边界，写在开关旁边，不假装能兜底。
- 多个 Mission Control 标签页同时开着时，同一段等待只出一条通知（同一个通知标签互相替换）。

## 4. 明确不做

- ❌ 风险规则升到「警示」——要先攒够标注数据、审完精确率。
- ❌ 手机推送与远程控制——保持搁置。
- ❌ 统计页冷启动缓存、敏感文件规则——记在债里。
