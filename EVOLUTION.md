# tokmon → Claude Mission Control · 逐代进化评估 (Evolution Retrospective)

> 这份文档不是 `CHANGELOG`（机械变更列表），也不是路线图（面向未来）。
> 它是一次**逐代的进化评估**：每一代因何而生、交付了什么、它的纪律动作是什么，
> 以及最关键的——**有多少是被现实验证过的，多少只是声称过的**。
> 目的是让人「感受并理解」这个系统是怎么长成今天这样的。
>
> 生成于 **2026-07-01**，由一次证据采集会话产出（方法论见 §0）。会话实跑了
> `pytest / doctor / report / backtest`，读了平台层与内核关键源码，grep 核了 import 图 / 只读性 / 定价集中。

---

## §0 方法论与可追溯性 —— 怎么重建的，能信到什么程度

**这不是 git 仓库**（`git: false`，无 `.git`）。所以**无法做 commit 级的逐次回放**。
本文的世代谱系是从**六个可交叉验证的考古层**重建的（单靠任一层都会失真，因为文档已在漂移，见 §4）：

1. 两份方向文档：`NORTH_STAR.md`（tokmon v0.x）、`MISSION_CONTROL.md` §9（平台 M0–M4）
2. `CHANGELOG.md`（**仅到 0.3.0，已停更**）
3. **代码自带的里程碑标记**：docstring / 注释里的 `M1 / M2 / M3.5 / M4`，以及「L2 实测发现」「评审发现」等修复记录
4. memory 决策记录（如 2026-06-29 定下「先加固信任」）
5. `report` 按天花费 = 客观活动时间线
6. 文件 mtime = 各模块最后触碰时刻

### 证据分级（每一代都会标）

| 级 | 含义 |
|---|---|
| 🟢 **实测** | 本次会话我亲自跑出来 / 单测钉住 |
| 🔵 **代码印证** | 读源码确认逻辑成立，但无运行时验证 |
| 🟡 **仅文档声称** | 文档这么说、代码也在，但行为未被独立核对 |
| 🔴 **未验证** | 声称「已交付」，却从未对真实世界验证过 |

### 客观时间线（文件 mtime）

> 注意：mtime = **最后触碰**时刻，不是创建时刻。跨代的修复会把早期模块的时间推后
> （例：`activity.py` 生于 M1，却因 M4.5 回测驱动的诚实修复，mtime 停在 06-30 04:04）。
> 所以这是「最后工作于」的顺序，用来感受节奏，不是严格的诞生序。

```
06-27 14:15  pricing / aggregate / util        ← 成本内核诞生
06-27 14:17  __main__
06-28 02:39  discovery / records
06-28 02:40  report / tui                       ← 两种形态（雏形完成）
06-28 02:50  parser / project                   ← Gen 0.2 项目身份
06-28 03:01  doctor                             ← Gen 0.3 数据契约体检
06-28 20:03  procmon                            ← M0 进程支柱（平台化起点）
06-29 02:55  event_sources/__init__
06-29 03:08  activity_source                    ← M2 成本无关适配器
06-29 12:23  control                            ← M4 控制层
06-29 14:53  notify                             ← M3 通知层
06-29 15:04  events                             ← M2 事件总线
06-29 15:05  cost_source                        ← M3.5 预算告警
06-29 20:47  inference_doctor                   ← M4.5 推断体检
06-30 03:41  cli
06-30 03:48  serve                              ← 平台整合外壳
06-30 04:04  activity                           ← M1 + 回测驱动的诚实修复
06-30 04:05  inference_backtest                 ← M4.5 准确率回测
06-30 04:06  test_tokmon                        ← 72 例测试
```

**最震撼的客观事实：整个代码库的 mtime 全部落在 06-27 → 06-30，约 3.5 天。**
叠加 `report` 数据（6/26–6/30 每天烧 $229–397，峰值 6/29 达 $397/天），这是一次
**极高强度、极度压缩的进化冲刺**——不是几个月的缓慢积累，而是三个半昼夜里长出的整座平台。

---

## §1 全景 —— 两个纪元，一次转身

进化有一条清晰的断层线，把它劈成两个纪元：

```
纪元一 · tokmon（成本感知工具）          【转身】              纪元二 · Mission Control（AI coding ops）
─────────────────────────────      ─────────────      ──────────────────────────────────────────
Gen0.1 雏形 → 0.2 项目身份 → 0.3 doctor   procmon 进程支柱      M1 活动 → M2 总线 → M3 通知 → M3.5 预算 → M4 控制 → M4.5 信任加固
「让我对烧的 token 有感觉」            serve 变成多监控外壳    「知道它在推进/卡住/烧钱/乱改，重要时刻主动找我」
北极星 = NORTH_STAR.md               ↑ 平台化在此发生      北极星 = MISSION_CONTROL.md
```

**转身点**（`NORTH_STAR.md:6-8` 明文记载）：2026-06 起，tokmon 从一个独立的 token tracker
**降格/升格**为更大平台 Claude Mission Control 的**「成本支柱」**。这一步在 `procmon.py`（06-28 晚）
落地——当 `serve` 开始承载第二个同级监控（进程/端口/隧道），它就不再只是 tokmon 了。

一条贯穿两个纪元的红线：**「真实优先 / 误报零容忍」**。每一代都有一个「宁可说不知道，也不瞎报」的
纪律动作。这条线是这个系统的人格，也是下面每代评估里最值得看的部分。

---

## §2 逐代评估

> 每代模板：**时间 · 使命 → 触发的痛 → 交付 → 纪律动作 → 证据[分级] → 刻意不做 → 埋下的种子/债**

---

### 纪元一 · tokmon

#### Gen 0.1 —「雏形」· 06-27→06-28 · *让我对烧的 token 有感觉*

- **痛**：vibe coding 时对「今天烧了多少 token / 多少钱」完全没感觉。
- **交付**：成本内核 `discovery / parser / pricing / aggregate / records` + 两种形态 `watch`(实时 TUI) / `report`(报表)。三层来源分类（main / subagent / workflow），按 `(message_id, requestId)` 全局去重，按模型家族定价 + 缓存 5m/1h/读 倍率。
- **纪律动作**：**未知模型标 `*` 而非瞎估**——`pricing.rates_for` 未知返回 `(0,0,False)`，宁可显示「未知单价」也不误导。
- **证据 🟢**：`doctor` 实测在 27,815 条真实记录上：三层拆分 main 55 / subagent 32 / workflow 393、去重 27815→8636（合并 69%，否则多算 ~3.7×）、3 个已知模型全覆盖。去重/定价/解析被单测钉住（在 72 例内）。
- **刻意不做**：无持久化（每次全量重扫）；watch 是定时重扫非文件监听；无预算告警。
- **种子/债**：项目名靠编码目录名猜（→ Gen0.2 解决）；全量重扫会随数据增长变慢（至今未解，SQLite 仍是 backlog）。

#### Gen 0.2 —「项目身份」· 06-28 · *知道是哪个项目烧的*

- **痛**：v0 用 `split('--')[-1]` 猜项目名，把空格变成 `-`，更深的 cwd 无法归并。
- **交付**：`project.py` 纯函数 `workspace_identity(cwd)`——项目 = `.vscode` 下的**直接子文件夹**，更深路径归并回去、余下存 `subpath`。`UsageRecord` 加 `cwd/subpath/under_vscode`，`--vscode-only` 过滤。
- **纪律动作**：**逐记录按各消息自己的 cwd 归属**（不是整文件用第一个 cwd）——会话中途 `cd` 不再错算。这是对抗式 review 发现的 HIGH 问题。
- **证据 🟢**：`report` 实测干净还原出真实项目名（`evolution-test / claude-token-monitor / news feed 0622 / API`，空格保留）；`doctor` 报 cwd 覆盖 100%。CHANGELOG 声称此修复修正了 home 会话约 33.7M tokens 的归属——此数字为**文档声称 🟡**（我无法回溯当时状态），但机制我已在代码中确认。
- **刻意不做**：不追求跟 Anthropic 真实账单分毫不差（算的是等价用量价值）。
- **种子/债**：项目身份现在**依赖记录里的 `cwd` 字段**——成为新的格式依赖（doctor 会盯它）。

#### Gen 0.3 —「数据契约体检 · doctor」· 06-28 · *让数字敢信*

- **痛**：整个系统依赖 Claude Code **未公开承诺的内部 JSONL 格式**（北极星 §5 标记的头号系统性风险）。需要在格式漂移**悄悄算错前**把问题暴露出来。
- **交付**：`doctor.py` + `tokmon doctor`——识别率 / 关键字段覆盖 / 去重健康 / **跨来源碰撞** / 未知模型，带 ✓/⚠/✗ 结论。
- **纪律动作**：**把「我的假设是否还成立」变成一条可运行的命令**。这是整个项目「诚实」哲学的第一次工具化——不靠信念，靠体检。
- **证据 🟢**：本次实跑，真实数据全绿（契约覆盖 ≈100%、**0 跨来源碰撞** → 退役了「来源拆分歧义」这条技术债）。
- **🟢 同时实测到一个真实 bug**：`doctor` 在 GBK 控制台直接崩（`UnicodeEncodeError: 'gbk' can't encode '✓'`，`doctor.py:258`）——rich 回退 legacy Windows 渲染时 `✓` 图标崩溃。**违反原则 5「渐进可降级」**：展示层遇编码问题应降级不该 crash。数据层已算完 480 文件才崩，反证了内核/展示分离是真的。
- **种子/债**：doctor 的「体检」范式，直接孕育了纪元二的 M4.5「推断 doctor」。

#### （桥）「serve 外壳」· 06-28→ · *浏览器里看趋势*

- **交付**：`tokmon serve`（**纯标准库 `http.server`**，零依赖、可离线，只监听 `127.0.0.1`、`no-store`）+ `/tokens` 页 + **「vs 上一周期」自基线对比**（回答北极星第三问「跟预期差多少」，基线是你自己的上一等长周期，不是预算）。
- **纪律动作**：诚实降级——`today` 比昨天同时段、`all` 无基线不瞎编、历史不足/未知单价均显式标注。
- **证据 🔵**：`serve.py` 结构与降级逻辑读码确认；此页面渲染我本次未逐一验证（🟡）。
- **意义**：这是「展示层不碰内核」（I6）的第一次跨形态验证——一个全新形态（web）没改任何内核。也是平台外壳的雏形。

---

### 转身 · M0 —「进程支柱 + 平台化」· 06-28 晚

- **痛**：agentic coding 的痛不止成本——进程健康、端口、隧道也想一眼看到。且「监控」这件事本身值得抽象成平台。
- **交付**：`procmon.py`（进程 / localhost 端口 / cloudflared 隧道 / 健康探测，**纯只读、脱敏**）成为 `serve` 主页上与 `/tokens` **并列的第二个同级监控** `/processes`。`MISSION_CONTROL.md` 诞生，确立三层架构（监控→事件→通知）与 8 条平台不变量（P1–P8）。
- **纪律动作**：**支柱独立**——procmon 完全不碰 token 内核。
- **证据 🔵**：import 实测干净（`procmon.py` 只依赖 stdlib + psutil，零 pillar 耦合）。运行时行为本次未验证（🟡）。
- **种子**：确立了「各支柱纯粹、平台只在事件层汇合」的骨架——纪元二全部建在这个骨架上。

---

### 纪元二 · Claude Mission Control

#### M1 —「对话活动 · 先看见」· 06-29（+06-30 修复）· *看见每个 session 在推进 / 等我*

- **痛**：Claude Code 在跑时，人不知道它在推进、卡住、还是等你。
- **交付**：`activity.py`（支柱独立：只 stdlib + discovery + project）从 transcript **尾部**推断每个 main session 状态：`WORKING / PROCESSING / AWAITING_USER / AMBIGUOUS_PENDING / UNKNOWN`。`/sessions` 页 = Mission Control 左栏雏形。**零通知**。
- **纪律动作（本项目最硬核的一处）**：
  - **时钟 = 最后一条*消息*的时间戳，绝不用文件 mtime**（元数据行会刷新 mtime，让 79h 没动的会话看着「刚活动过」）。
  - **拿不准就 AMBIGUOUS_PENDING/UNKNOWN，绝不伪造 `PERMISSION_NEEDED` 或断言 `STUCK`**——`classify_state` 里久未返回统一并列「可能长任务 / 等授权 / 会话已关闭（transcript 无法区分）」。
  - `classify_state` 是纯函数；代码里带着 **4 处「L2 实测发现」的诚实修复**（如 `activity.py:137-139`：thinking-only 片段是流式中途而非回合结束，实测 173/173，否则会误报 `TASK_COMPLETED`）。
- **证据 🟢（全代最强）**：`backtest` 用 transcript 的真实后续当真值实测——**准确率 97.0% / 1343 个评估点 / 覆盖 99%**，最伤信任的「危险格」（说在干活实则已停 / 假完成）**各仅 1 例（≈0.15%）**。`inference_doctor` 报 UNKNOWN 率 0/55。读码 + 单测双重印证。
- **刻意不做**：不做 tool_use↔tool_result 全局配对（已验证脆弱），只看尾部位置。
- **种子**：真实检测出的 permission 缺口留给 M4；准确率如何证明留给 M4.5。

#### M2 —「事件总线」· 06-29 · *把信号变成解耦的事件*

- **痛**：监控层若直接调通知，后面必成屎山（判断「发生了什么」必须与「该不该打扰」解耦）。
- **交付**：`events.py`（**支柱无关的中立基础设施**：Event + 严重度表 + §6 payload allow-list + 有界 EventBus）+ `activity_source.py`（纯 `derive_events` + 唯一只读 pump）。事件：`SESSION_STARTED/TASK_COMPLETED/TOOL_ERROR/SESSION_IDLE/SESSION_STUCK`。
- **纪律动作**：
  - **隐私写进构造函数**——`Event.make`（`events.py:72`）在建事件时就用 allow-list 把 `last_text/title` 等会话正文过滤掉。哪怕生产者想塞敏感内容也进不了事件。
  - **两层幂等**：源头边沿触发 + 总线 dedup_key 窗口，且 `move_to_end` 刷新（持续越线的键不会老化出窗后又重响）。
  - **本模块 import 任何 pillar 都不允许**。
- **证据 🟢/🔵**：import 实测 `events.py` 只依赖 stdlib（P4 铁律成立）；幂等/边沿/UNKNOWN 恢复被单测钉住（在 72 例内）；`since()` 游标截断不丢事件的逻辑读码确认。
- **刻意不做**：总线**无任何 §7 防打扰策略**——全留给 M3 消费者边。
- **种子**：一个「加事件类型不用改 UI/通道」的中立契约——M3/M3.5/M4 全部零改动挂上。

#### M3 —「通知层 · 有用且不烦」· 06-29 · *重要时刻主动找我*

- **痛**：没在看屏幕时，重要的事得主动找到我；无关的事得安静。一个让人想关掉的通知系统是负价值。
- **交付**：`notify.py`（总线**消费者**，只 import stdlib + events）。纯函数 `decide`：严重度门控 → 静默时段（只放行 critical）→ 去抖 → 限流。后台 sender 线程发 Telegram，不阻塞总线。`/notify` 页含**推送/抑制双记的 feed**（方便调「不烦」）。
- **纪律动作**：
  - **critical 绕过去抖和限流**（`notify.py:125-131`：「重要的事必须主动找到你」）。
  - **默认不外发**：没配 token 就 `delivery="未配置(仅本地)"`，**一字节不出本机**（首个 egress 的显式 opt-in）。
  - 内容最小化 + **token 绝不回显**（status 只报「已配置/未配置」）。
- **证据 🔵 / 🔴（关键分裂点）**：策略逻辑 `decide` 读码确认 + 纯函数单测（🔵）。但**外发从未对真实手机验证过**（🔴）——`send_test` / sender 线程走真实 Telegram API 的路径，本次无 token 未触发。
- **意义**：这是平台**唯一会外发**的能力，也是北极星承诺（「主动找到我」）的兑现物——却停在未验证。

#### M3.5 —「成本预算告警」· 06-29 · *闭环 tokmon 第三问*

- **痛**：tokmon 北极星第三问「跟预期差多少」，此前只有「vs 上一周期」，没有**主动预算**。
- **交付**：`cost_source.py`（成本支柱适配器：stdlib + events + 成本内核）。日/周/每项目预算，越过 70/90/100% 发 `TOKEN_BUDGET_WARNING` → 通知。60s pump。
- **纪律动作**：
  - **无状态幂等**——dedup_key 含 `(scope, 周期, 阈值)`，同周期同阈值只响一次，跨日/跨周自动重置，不用 memo。
  - `≥90%` 判 critical（绕过去抖）。事件只带 `{scope, pct}` + 项目名，绝不带花费明细。
  - `_clean` 处理手改进 json 的 `Infinity/NaN`。
- **证据 🔵 / 🔴**：`cost_source.py` 读码确认逻辑完整且接线（`serve.py:336` 起 pump），是**唯一不可能误报**的通知（成本是 ground truth）。但同 M3——**告警送达手机从未验证**（🔴）。
- **⚠ 文档漂移**：`MISSION_CONTROL.md` §9 把 M3.5 列为「backlog 已选」，**但代码已完整实现**。见 §4。

#### M4 v0 —「控制层 · 远程审批」· 06-29 · *手机远程 allow/deny*

- **痛**：人离开时，Claude Code 卡在等 permission = 进度被你阻塞，却没法远程放行。
- **交付**：`control.py`（只 import stdlib + events + 纯 project 工具）。经研究发现 Claude Code 有官方 `PermissionRequest` **http hook**——干净地远程 allow/deny，无需脆弱的按键注入。hook → 本服务 `/hook/permission`：检测时发**真实 `PERMISSION_NEEDED` 事件**（补上 M1 测不准的缺口），远程模式下阻塞等你在 UI/手机点。
- **纪律动作（全程 §8 P7）**：
  - **失败安全铁律**：`handle_permission` 里 bad-token / local-mode / overloaded / timeout / exception **每一条非正常路径都 defer 回退本地弹窗**，**绝不自动 allow/deny、绝不伪装成功**。
  - allow-list（只做 permission 审批）、你显式点击才决定（超时=defer 非决定）、token 鉴权（`secrets.compare_digest` 常量时间）、全审计 + `COMMAND_ISSUED` 事件、token 0600 原子写不回显。
- **证据 🔵 / 🔴**：失败安全的 defer 分支我逐条读码确认（🔵，非常干净）。**但远程审批端到端从未对真实 Claude Code 验证过**（🔴）——`control.py:192` 作者**自己在代码注释里诚实标注**：hook 回包形状「确切字段以真实 Claude Code 文档/实测为准，这里取已记录的形状；defer 永远安全」。即：正路（远程 allow 被真实 Claude Code 认）未验证，只有安全兜底（defer）是确定的。
- **刻意不做**：nudge/停止研究确认**无干净 API** → 按失败安全原则**不做**（不靠脆弱方式硬来）。

#### M4.5 —「信任加固 · 推断 doctor + 准确率回测」· 06-29→06-30 · *量化证明推断敢信*

- **痛**（memory 2026-06-29 决策）：平台价值全压在 Layer 1 判断力上，而判断力此前**只有信念没有度量**。决定「先加固信任，再拓宽」。
- **交付**：
  - `inference_doctor.py` —— doctor 精神从「成本契约」扩展到「**推断契约**」：时钟假设 / 行类型漂移 / stop_reason 完整性 / 挂起判定回配率 / thinking 可用性 / 状态分布 / UNKNOWN 率。
  - `inference_backtest.py` —— 用 transcript 的**未来当真值**回测 `classify_state`，**三把尺**（strict/medium/lenient oracle）分别报准确率 + 混淆矩阵 + 危险格。
- **纪律动作**：**把「敢不敢信」从形容词变成数字**。这是 Gen0.3 doctor 范式的最终形态——不仅体检数据，还体检自己的推断。
- **证据 🟢（本次实跑，全代证据最实）**：
  - inference doctor：55 主会话，行类型无未知、stop_reason 全覆盖、挂起回配 794/796、**UNKNOWN 率 0/55**，结论「推断假设健康」。
  - backtest：三把尺 **96.9–97.0% 准确率 / 99% 覆盖**，危险格各 1 例。附带实证「29 个会话 mtime 比末条消息新 >30min」——正是「不能用 mtime 当活动时钟」的铁证。
- **⚠ 文档漂移**：M4.5 **整个不在 `MISSION_CONTROL.md` §9 路线图里**，却已进 `cli.py`（`doctor` 串跑 inference doctor、独立 `backtest` 命令）。见 §4。

---

## §3 整体评估 —— 已建 vs 已验证

> （承接会话中对整体进化的总结，钉入文档。）

**作为一次进化，它的完成度和纪律性都超出预期，但重心失衡。**

- **强**：不只把 `observe → event → notify → control` 四层建齐并**真装配起来**（`serve.py:335-337` 一次启动三个 pump + hook 端点，不是孤立模块），还额外长出一整套**信任基建**（数据 doctor + 推断 doctor + 准确率 backtest），并用 **97% 准确率 / 0.15% 危险误判 / 100% 契约覆盖** 量化证明了自己敢信。诚实与失败安全是**写进代码、grep 得到、单测钉住**的，不是文档口号。这在个人工具里相当罕见。
- **弱**：兑现北极星的「最后一公里」——**真的把重要的事推到手机、让你远程处置**——是整条链上**唯一没被现实验证过的一环**，且作者自己在代码里标了这个不确定性。

**一句话：机器造好了、且造得很干净，但还没在真实负载下通过电。**

### 证据分级的全景（这是最该「感受」的一张图）

```
纪元一 tokmon            纪元二 Mission Control
观测 / 度量 / 信任        通知 / 控制（外发与入站）
━━━━━━━━━━━━━━━━━━━      ━━━━━━━━━━━━━━━━━━━━━━━━━━
Gen0.1 内核      🟢       M1 活动       🟢
Gen0.2 项目身份  🟢       M2 总线       🟢/🔵
Gen0.3 doctor   🟢       M3 通知       🔵→🔴（外发未验证）
serve 外壳      🔵/🟡     M3.5 预算     🔵→🔴（送达未验证）
M0 procmon      🔵/🟡     M4 控制       🔵→🔴（远程审批未验证）
                         M4.5 信任加固 🟢
```

**左墙一片绿（观测/度量/信任），右缘一道红（外发/控制的真实兑现）。** 这不是巧合，而是这次进化的形状：
它把「敢不敢信」这件事做到了可测量的 97%，唯独没测的，正好是价值最高的那一下——「有用且不烦」里的「有用」。

---

## §4 这份文档暴露的一个元问题 —— 文档在对代码漂移

有点反讽：这个项目的头号风险管理手段就是「用 doctor 让格式漂移**可被发现**」，
而它自己的**方向文档正在对代码漂移，且没有 doctor 去发现**：

| 文档 | 停在哪 | 与代码的真实差距 |
|---|---|---|
| `CHANGELOG.md` | **0.3.0（doctor）** | 之后 serve / M1 / M2 / M3 / M3.5 / M4 / M4.5 **全没记**（漂移 ~8 个切片） |
| `MISSION_CONTROL.md` §9 | M4 v0 已交付、M3.5/M5 backlog | **M3.5 其实已实现**；**M4.5 整个不在路线图**却已进 cli |
| `NORTH_STAR.md:127` | `report --by/--json`「下一个待做」 | **被跳过了**——cli 的 report 只有 `--since`。项目从「打磨 report」直接跳到整个平台 |

外加两个开箱裂缝：
- `requirements.txt` **没有 pytest**，`unittest discover` 发现 **0 个测试**——任何人照 requirements 装会以为「没测试」。装了 pytest 才 72 passed。
- `doctor` 的 GBK crash（§Gen0.3）。

这些不是能力问题，是**记录与现实脱节**的问题。对一个以「诚实/可信」为灵魂的系统，值得用它自己的哲学对待：让漂移可被发现。

---

## §5 怎么用这份文档

- **想感受进化节奏** → 读 §0 时间线（3.5 天）+ §1 两纪元图。
- **想判断某一代能不能信** → 看那代的**证据分级**，🟢 可依赖、🔴 别当已完成。
- **想知道下一步** → §3 的「左绿右红」直接指向：**先给右缘那道红通电**（真机验证 M3/M4 手机闭环），而不是再拓宽。
- **想修裂缝** → §4 列了四个具体的、低风险的对齐/修复点。

> 这份评估本身也会漂移。下次大改后，最诚实的做法是**重跑一遍** `pytest / doctor / report / backtest`，
> 用真实输出刷新 §2 的证据分级——而不是相信这份文档（包括这句话）。
</content>
</invoke>
