# 插队小切片 · `/sessions` 过滤器 — 设计草图

> **【状态章 · 2026-09-20 核对】✅ 已实施（V1）。** 后续的 V2 从未实施、V3 亦未实施；
> 本页真正的诚实缺陷（默认藏掉「等你」的会话）已在 0.9.0 用最小切片修掉。

> 给主程序的实现交接。本文件是**设计与约束**，不是最终代码。惯例同 [M4.5_PLAN.md](M4.5_PLAN.md)。
> **规模自觉**：这是个**小切片**，文档也该小。它唯一的价值是**钉死三个容易写错的坑**（§3）——别的都是顺手。

> **三个已敲定的决定（2026-07-13）：**
> 1. **filter 维度** = **项目（必须）+ 状态 + 活跃时间窗**。（不做模型、不做 git 分支——现在不需要）
> 2. **项目 filter 形态** = **多选 chips**（可同时看几个项目）。
> 3. **持久化** = **记住**（`localStorage`），跨刷新与重开页面都保留。

---

## 0. 现状（读码实证，`serve.py`）

| 事实 | 位置 |
|---|---|
| `/sessions` 页 = `SESS_PAGE` 大字符串 | `serve.py:1127` |
| **过滤本来就是纯客户端的** —— `render()` 对已取回的 `DATA` 做 filter | `serve.py:1267-1270` |
| `/api/sessions` 返回**全量快照**，无服务端过滤 | `serve.py`（`activity.snapshot`） |
| 已有控件：文本搜索 `#filter`、隐藏空闲 `#hideidle`、自动刷新 `#auto`(30s)、`#refresh` | `serve.py:1180-1185` |
| 已有统计 pill（活跃/后台/久未返回/等你/空闲/已关闭/读不出/共） | `serve.py:1257-1265` |
| 已有「显示 N/M」提示 `shownNote` | `serve.py:1297` |
| 每个会话可用字段 | `project` / `subpath` / `git_branch` / `model` / `title` / `state` / `state_label` / `idle` / `last_activity_age_s` / … |

### 🎯 关键结论：这一片**不用碰任何后端**

因为过滤已经是客户端的 —— 本切片**零 API 改动、零 `activity.py` 改动、零内核改动**，
全部工作在 `SESS_PAGE` 的 HTML + JS 里。**I6（展示层不反向污染内核）天然守住。**
这也是它能「插队」的原因：风险面极小。

---

## 1. 状态模型（**单一真相源**）

```js
let FILTERS = {
  projects: [],      // [] = 全部 (不筛); 否则是选中的项目名数组 (多选)
  states:   [],      // [] = 全部; 取值见 §2 的状态分组
  ageMax:   null,    // null = 全部; 否则 3600 / 86400 / 604800 (秒)
  text:     '',      // 复用现有文本搜索
  hideIdle: true,    // 复用现有「隐藏空闲」
};
```

**🔑 铁律：`FILTERS` 是唯一真相源，DOM 只是它的投影。**
所有筛选读 `FILTERS`，不读 DOM。持久化到 `localStorage['mc_sess_filters']`（存/取都 try/catch，坏数据 → 回退默认）。

---

## 2. 过滤管线（纯函数，逻辑集中在一处）

```js
function applyFilters(sessions, F) { ... }   // 纯函数: (数组, 条件) -> 数组。不碰 DOM, 不改入参。
```

顺序（**AND 关系**，逐级收窄）：

1. **项目**：`F.projects.length === 0 || F.projects.includes(s.project)`
2. **状态**：`F.states.length === 0 || F.states.includes(groupOf(s.state))`
3. **时间窗**：`F.ageMax === null || (s.last_activity_age_s != null && s.last_activity_age_s <= F.ageMax)`
   > ⚠️ `last_activity_age_s` 可能是 `null`（读不出）。**`null` 在有时间窗时应被排除**（因为它「不可判」），
   > 但**不能悄悄排除** —— 见 §3.3 的诚实提示。
4. **隐藏空闲**（现有）：`!(F.hideIdle && s.state === 'AWAITING_USER' && s.idle)`
5. **文本**（现有 `matches()`）

**状态分组**（对齐现有 `CLS` 映射 `serve.py:1234`，别自造第二套语义）：

| 分组 | 对应 `state` | 现有 pill |
|---|---|---|
| `live` | `WORKING` / `PROCESSING` | 活跃 |
| `wait` | `AWAITING_USER` | 等你 |
| `amb` | `AMBIGUOUS_PENDING` | 久未返回 |
| `closed` | `CLOSED` | 已关闭 |
| `unk` | `UNKNOWN` | 读不出 |

---

## 3. 🔴 三个必须钉死的坑（**本文件的全部价值在这**）

### 3.1 💣 30s 自动刷新会把 chips 重建 → 选中态丢失

页面每 30s 跑一次 `render()`。项目 chips 是**从快照动态取 distinct project** 生成的，
所以每次 render 都会**重建这段 DOM**。

**如果把「选中」存在 DOM 上（比如靠 chip 的 CSS class），它会被重建冲掉** ——
表现为：你选了项目，30 秒后自己跳回「全部」。**这是本任务最容易写错的地方。**

✅ **解法**：选中态存在 `FILTERS.projects`（JS 变量 + localStorage），
每次重建 chips 时**从 `FILTERS` 恢复选中样式**。DOM 是投影，不是存储（§1 铁律）。

> 顺带：现有的 `#filter` / `#hideidle` 之所以没这问题，是因为它们是**没被重建的原生 input**。
> 新的 chips 是动态生成的，**规则不一样**。

### 3.2 🟠 选中的项目**消失了**（会话轮转 / 那个项目暂时没会话）

如果 `FILTERS.projects = ['API']`，但这一帧快照里 `API` 一个会话都没有：

❌ **绝不能**静默把它当「没选」→ 显示全部。
   那会让你以为「API 没有会话了」，其实是**筛选被悄悄取消了**，你看到的是全部——**误导**。

✅ **必须**：保留该 chip（标灰 / 标「0」），列表正常显示「无匹配会话」，
   并在提示区写明「**项目 API 在本帧无会话**」。**宁可啰嗦，不可误导**（原则 1 的 UI 版）。

### 3.3 🟠 过滤会藏东西 → **必须让人始终知道自己在看子集**

现有 `shownNote`（`serve.py:1297`）已经是这个机制的雏形（「显示 N/M」）。加了 filter 后**必须升级**：

```
显示 3/55 · 过滤中：项目=API, news feed · 状态=活跃 · 近 24h · [清除全部过滤]
```

- **只要有任何 filter 生效，就必须醒目提示**（不是灰色小字，要看得见）。
- **一键「清除全部过滤」**。
- 若时间窗排除了 `last_activity_age_s == null` 的会话，**要在提示里说**（如「另有 2 个会话时间读不出，已被时间窗排除」）。

> **为什么这条这么重要**：决定 3 是「记住 filter」。这意味着**你明天打开页面，可能只看到 3 个会话，
> 却忘了自己昨天筛过**。持久化和「不误导」是一对矛盾——**靠这条醒目提示 + 一键清除来化解**。
> 没有这条，持久化就是个陷阱。

---

## 4. UI 落点（尽量复用，别加一堆新控件）

- **项目**：现有 `.controls` 行下面加一行 chips（从快照 distinct project，按会话数降序）。点击切换选中。
- **状态**：✅ **推荐把现有的统计 pill 变成可点的**（`serve.py:1257-1265`）——
  它们本来就在显示每个状态的计数，**点一下就筛该状态**。**零新控件、直觉、还省地方。**
  （备选：单独一行状态 chips。但 pill 复用更优雅。）
- **时间窗**：一个小 `<select>`：全部 / 近 1h / 近 24h / 近 7d。
- **提示区**：把现有 `shownNote` 升级成 §3.3 的样子。

---

## 5. ⚠️ 诚实记录：**这一片没有单测**

**为什么**：它是**纯前端 JS**（写在 Python 的 HTML 大字符串里），项目**没有 JS 测试设施**。
硬要测就得把逻辑搬到服务端（破坏 §0 的「零后端改动」优势）或引入 Node 测试栈（破「零额外依赖」）。

**这是一个已知的、被接受的缺口——写下来，不假装有覆盖。**

**补偿措施：**
1. `applyFilters` 写成**纯函数**（不碰 DOM、不改入参），逻辑集中在一处，易读易审。
2. **真机冒烟**：起 `tokmon serve` → 开 `/sessions` → 逐条验 §6 的完成判据（尤其 **3.1 的 30s 不丢选中**，
   这个必须**真的等 30 秒**看它不跳回全部）。
3. 若未来这页 JS 继续长大，再单独决策要不要引 JS 测试设施（**不在本切片**）。

---

## 6. 完成判据

1. 能按 **项目（多选）+ 状态 + 时间窗** 筛，三者 **AND** 收窄，且与现有的文本搜索/隐藏空闲共存。
2. **🔴 等满 30s 自动刷新后，选中的项目 chips 不丢**（§3.1 —— 必须真的等，别只点一下就说好了）。
3. **重开页面后 filter 仍在**（localStorage）。
4. **任何 filter 生效时都有醒目提示 + 一键清除**；选中项目在本帧无会话时**不静默变全部**（§3.2/3.3）。
5. **零 API / 零 `activity.py` / 零内核改动**（`git diff` 只应碰 `serve.py` 的 `SESS_PAGE`）。

---

## 收尾：沿用既有纪律

`design → implement → 真机冒烟（本片无单测，见 §5）→ 对抗式 review → 修确认项`。
先问北极星：**这让「我一眼看出每个 session 在推进 / 卡住 / 等我」更快了吗？**
——55 个会话里大半是已关闭的噪音，**能按项目 + 状态收窄，正是让那个「5 秒内回答」真正成立的东西。**
