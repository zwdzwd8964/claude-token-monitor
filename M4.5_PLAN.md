# M4.5 · 收口固信 — 剩余工作设计草图

> **【状态章 · 2026-09-20 核对】🟡 部分实施。**
> **L1（漂移体检）✅** `inference_doctor.py` · **L2（准确率回测）✅** `inference_backtest.py`（实测 97.0%）。
> **L3（事件完整性回放）/ L4（打扰预算）❌ 仍未做。**
> **§B 手机闭环**：Pushover 通道 ✅ 已于 0.9.0 写好（默认关、内容最小化、优先级映射有单测），
> 但**真账号送达仍未验证**——这是整座塔上唯一还没通过电的一环。

> 给主程序的实现交接。本文件是**设计与约束**，不是最终代码。
> 上层方向见 [MISSION_CONTROL.md](MISSION_CONTROL.md) §9 的 M4.5；本文件只展开**尚未交付的部分**。
>
> **两个已敲定的决定（2026-06-30）：**
> 1. 推断 doctor **四个 lens 全做**。
> 2. 手机闭环（half b）**先用 Pushover/Email 替代 Telegram**——更快让手机真的震起来；但记住它**只能出、不能收**（见 §B 的诚实边界）。

---

## 0. 现状：哪些已做、哪些没做

**✅ 已交付 = L1（漂移 / 载重假设量化）** —— `tokmon/inference_doctor.py`：
对每条载重假设做**静态快照式**体检（时钟 / 行类型 / stop_reason / 挂起判定 / 思考可用性 / 打断形态 / 标题 / UNKNOWN 率），
带 ✓/⚠/✗ 结论，接入 `tokmon doctor` 第二半 + `/doctor` 页。**只读尾部（复用 `activity._read_tail`）、只跑一次 `classify_state`。**
首跑即抓修 3 件真事（custom-title / max_tokens / thinking 非空）。这是"升级 Claude Code 后先跑"的漂移警报——**保留，不动。**

**❌ 尚未做 = L2 / L3 / L4。** 共同原因：L1 故意只看尾部、只看"此刻"；而 L2/L3/L4 都要**回放历史**——
要么用一条 transcript 的"未来"当真值，要么把整条事件流重演一遍。**读取纪律不同**（见 §1.1），所以是独立构件，不是给 `scan_inference` 再加几个计数器。

---

## 1. 通用约束（三个 lens 都遵守）

- **位置 & P4**：L2/L3/L4 可放进 `inference_doctor.py`，或拆 `inference_backtest.py` 兄弟模块。它们**位于支柱之上**，
  只**只读 import** `activity` / `events` / `event_sources` / `notify`；**任何支柱都不许 import 它们**（依赖箭头单向，和 serve 一个待遇）。
- **只读 + 不外发**：只读 transcript、只跑纯函数回放；零写、零网络。
- **doctor 吃自己的狗粮（原则 1）**：凡是判不了真值的样本（见 L2），**单独计入"不可判"并显式报出**，
  **绝不**把它们掺进准确率分母去凑一个好看的数字。一把没校准的尺子比没有尺子更危险。
- **纯函数 + 自带单测**：真值推导、回放、计分都做成纯函数；用"构造时就知道答案"的合成 transcript 钉死（否则就是没校准的尺）。

### 1.1 读取纪律（与 L1 的关键区别）
L1 复用 `activity._read_tail`（目的是"验证 activity 实际所见"）。**L2/L3/L4 需要更全的历史**：
- L2 要 T 时刻之后的"未来"当 oracle；
- L3 要把整条消息流增量喂给 `derive_events`；
- L4 要一段时间窗（如近 7 天）的完整事件流。

→ 需要一条**独立的全量/窗口读取路径**（流式、内存有界；可按"近 K 天"裁剪以控成本）。不要硬塞进 `_read_tail`。

---

## 2. L2 · 准确率回测（时间旅行）— 剩余工作的肉

**目标**：量出 `classify_state` 在过去某一刻的判断**与真实发生的下一步是否一致**。零人工标注——transcript 自己的未来就是 oracle。

**机制**：对每条主会话 transcript，取有序消息列表 `msgs`。在若干截断点 `i`（可抽样以控成本）：
1. `prefix = msgs[:i+1]`，`T_i = ts(msgs[i])`；按 `_read_tail` 的规则从 `prefix` 取尾部 → 得到活的分类器在 T 时刻会看到的 `last`。
2. **评估时刻取 `now = ts(msgs[i+1]) − ε`**（successor 到达前最后一刻——最公平的测试点）；`P = classify_state(last, now, cfg)`。
3. **从 successor 推真值标签 TRUTH**（令 `G = ts(msgs[i+1]) − T_i`，`idle_thr` 与活配置一致）：

   | successor 类型 | gap | TRUTH |
   |---|---|---|
   | user 消息 | 任意 | 当时在**等用户**；`G > idle_thr` → **idle** |
   | assistant / tool_result | 小 | 当时在 **WORKING/PROCESSING** |
   | assistant / tool_result | 大 | **长任务在飞**（对冲恰当，记诚实对冲正确） |
   | 无 successor + 文件很旧 | — | 会话**已结束/被弃** |
   | 无 successor + 文件新 | — | **不可判**（它此刻真的在飞）→ 计入"不可判"，**不进分母** |

4. **计分**：建混淆矩阵 `预测 P × TRUTH`。重点盯两个危险格：
   - **预测 WORKING，但 TRUTH = 等待/已结束** = **过度乐观**（说"在干活"其实早停了/在等你）。最伤——会让你以为它在推进。
   - **预测 idle/AWAITING，但 TRUTH = working** = **假完成**（说"完事了"其实还在跑）→ 会触发错误的"任务完成"通知 = 打扰且错。
   - `AMBIGUOUS_PENDING`：只要真实结局落在它并列的三种（长任务最终完成 / 等授权 / 关闭）之内，**记对冲正确**，不苛责没说死。

**产出**：每状态精确率/召回 + 混淆矩阵 + 两个危险格计数 + **可判/不可判拆分**。这是 M4.5"敢信"的核心证据。

---

## 3. L3 · 事件完整性回放（`derive_events`）

**目标**：把 M2 的诚实纪律从 9 条 fixture 升级到**全量真实语料**上验证，并和现实对账。

**机制**：把每条会话的消息流**增量**喂给 `event_sources/activity_source.derive_events`（模拟 pump 看到不断增长的尾部），收集事件序列。

- **不变量回放（必须全 0 违例）**：
  - 幂等：同一边沿不重复发事件（同 `dedup_key` 不重出）。
  - stepped-idle 单调：`SESSION_IDLE` 台阶只增不减。
  - 冷启动基线静默：首次观察一条已经很长的会话，**不**回灌一串补发。
  - `UNKNOWN → 不发`；**从 UNKNOWN 恢复不重放**（M2 修过的 bug，在全量上确认它仍不复发）。
- **对账现实（Option B）**：每个 `TASK_COMPLETED` 对得上一次真实"回到等用户"；每个 `TOOL_ERROR` 对得上一条真实错误 tool_result；**没有无据之事件**。

**产出**：不变量违例计数（应为 0）+ "已发事件 vs transcript 可证事件"对账表。

---

## 4. L4 · 打扰预算（回放过 `notify.decide()`）— 通往 half (b) 的桥

**目标**：在你还没接手机前，就把"不烦"调好。

**机制**：取 L3 重演出的事件流（近 7 天窗口），按 ts 顺序喂进一个**全新的 `decide()` 状态机**，合成时钟 = 事件 ts
（`decide` 用 msg-ts 而非 `now()`，故回测忠实）。

**产出**：
- "过去一周这台机器手机**会震 N 次**"——按 severity / 类型 / 会话 / 天 拆开。
- "被抑制 M 条，各因为什么（去抖 / 限流 / 静默时段 / 严重度门控）。"
- 高亮**最吵的源**（哪个类型/会话占了大头）= 该调的东西。
→ 这张表就是 (b) 的**预调参起点**：调到预测频率可接受，再开真实通道。

---

## B. half (b) · Pushover/Email 替代闭环

**构件（小）**：往 `notify.py` 加一个**薄 Pushover 出站适配器**，与 Telegram sender 并列、可插拔：
- **默认关**：没配 `MC_PUSHOVER_TOKEN`（app token）+ `MC_PUSHOVER_USER`（user key，env 或 `~/.tokmon/notify.json`）→ 一字节不出本机（守 P5）。
- **内容最小化**：同 Telegram 的 wire 纪律——类型/项目/指标/严重度，**无命令行/路径/密钥**（§6）。
- **severity → Pushover priority**：Critical → 高优先级（必要时 emergency+retry）；Warning → 0；Info → 不推。
- 推荐 Pushover 先行（出站就一个 POST、无需 BotFather、最快让手机震）；**Email digest** 作可选 fast-follow（SMTP，日摘要：任务完成/超预算/崩溃），适合摘要不适合实时。

**诚实边界（必须记牢）**：Pushover/Email **只能出、不能收**。
→ 它能闭"**通知出**"那半环（验证"有用且不烦"）；
→ "**远程批 permission（控制入）**"这半在 M4.5 里**走网页 `/control`**（localhost；或经 cloudflared 隧道暴露——那要权衡入站攻击面 vs Telegram 的零端口 getUpdates）；
→ 真正"手机消息里点允许/拒绝"**显式推迟到 Telegram 双向**，不在本切片硬来。

**验证（运维，约一周）**：开 Pushover → live 一周 → 用 M3 已有的 `/notify` 推送/抑制 feed + L4 回测**对账预测 vs 实际** → 调阈值。

---

## C. M4.5 完成判据（据本次两决定收紧）

1. **推断 doctor 四 lens 齐**：L1 ✅；L2 给出每状态精确率 + 混淆矩阵 + 可判/不可判拆分；L3 全量 0 违例 + 事件对账平；L4 给出"手机一周会震 N 次"预算表。
2. **通知出闭环为真**：Pushover 通道 live，手机真的震过；打扰率经一周实测、与 L4 对账后可接受。
3. **远程审批**：经网页 `/control` 验证可用（localhost/隧道）；**手机消息批准/拒绝显式留待 Telegram**。

---

## D. 落地次序建议

1. **L2** 先（最值钱、也最费工，且独立可交付——今天就能告诉你分类器到底准不准）。
2. **L3** 次（不变量回放便宜，多复用 L2 的全量读取路径）。
3. **L4** 再（依赖 L3 的事件流）→ 顺势产出 (b) 的预调参表。
4. **Pushover 适配器 + 一周验证**（b）：在 L4 给出理性起点后接手机。

每一步沿用既有纪律：design → implement + 单测 → 对抗式 review → 修确认项。
