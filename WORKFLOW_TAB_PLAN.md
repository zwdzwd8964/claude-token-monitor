# `/workflow` · 工作流追踪器 — 一页规格

> **【状态章 · 2026-09-22】S1 ✅ 已验收（0.10.0）· S2 ✅ 已交付（0.11.0），待你验收 → S3。** 详见 CHANGELOG。
>
> ~~状态：待你确认 → 确认即开工 S1。~~ 来源：2026-09-21 六轮问卷（23 项决定）+ 3 次 transcript 探针（数据事实均已实测）。
> 刻意只写一页：决策 + 边界 + 判据。实现细节在代码与单测里，不在这里（吸取 SESSIONS_V3「1164 行设计 0 行实现」的教训）。

## 0. 北极星（你的原话）

> **能追踪、能看懂、能学到整个任务是怎么完成的；不缺信息，也不堆信息。**

落地规则 = **分层披露**：L0 结构化摘要 → L1 树 + 瀑布 → L2 明细面板。每层只放「这一层该回答的问题」，其余点开才有。

## 1. 决策表

| 维度 | 决定 |
|---|---|
| 位置 | 独立 `/workflow` tab（导航紧跟「Token 看板」）；`/tokens` 的项目行可深链进来 |
| 子页 | **回放**（主视角）｜**统计** |
| 任务单位 | **一次真人提问**。后台启动、跨轮才完成的 agent / workflow 归到启动它的那一轮，标「延续至 #N」 |
| 导航 | 左栏任务列表：时间倒序、运行中置顶；项目 · 提问首句 · 耗时 · token · 异常徽章；可搜索、按项目筛；默认近 7 天（今天/7/30/全部） |
| 时效 | 事后复盘；选中的任务若仍在跑，每 5s 自动刷新 |
| 追踪对象 | 内置工具 · Skill（含归属）· MCP · 子 agent / Workflow 层级 |
| 概览 L0 | 结构化摘要：总耗时三段 + 计数 + 异常计数 + 关键时刻（全部真值，**不调模型**） |
| 回放 L1 | 左：可折叠调用树；右：对齐的瀑布时间轴 |
| 默认展开 | 展开到 skill / agent / phase；工具调用折成计数；**含异常的分支自动展开到出事那一步** |
| 穿插内容 | Claude 调用间说的话 · 思考片段（有才显示）· 系统事件（默认折叠）· 你的插话/打断（一行标记，悬浮看内容） |
| 明细 L2 | 完整输入输出 · 长内容折叠可展开/复制 · 敏感值脱敏（复用 procmon 的脱敏） |
| token | 调用级：**双数分列，标 ≈**（发起成本 + 结果体积）；agent / skill / workflow / 任务级：**真值** |
| 单位 | 行内只显示 token；悬浮显示等价 $ 与「缓存读 / 缓存写 / 新输入 / 输出」拆分 |
| 耗时 | 三段：**机器执行 / 模型生成 / 等你**，三色分开 |
| 异常高亮 | 失败 · 重试与原地打转 · 异常慢 · 超大结果 |
| MCP 关系 | 谁调的（真值）· 先后与并行（真值）· 数据依赖（**推断**）· 服务健康面板 |
| 学习辅助 | 名词悬浮说明 · Workflow 脚本对照 · 阶段自动标注（**推断**）· 跨任务对照 |
| 导航清理 | `/notify` `/control` 从导航隐藏，代码与 URL 保留 |

## 2. 数据从哪来（全部实测过）

| 要的东西 | 来源 | 性质 |
|---|---|---|
| 任务边界 | user 行 `origin.kind == "human"`；`task-notification` 并入启动它的任务；`isMeta` / `isCompactSummary` / 本地命令不算任务 | 真值 |
| 层级 | `Workflow` 结果的 `runId` → `subagents/workflows/<runId>/agent-*.jsonl` + `.meta.json`（`description` / `workflowPhase`）；`Agent` 结果的 `agentId` → 子文件 | 真值 |
| Phase 与脚本 | `<会话>/workflows/scripts/<name>-<runId>.js` 头部的 `meta.phases[{title, detail}]` | 真值 |
| Skill 归属 | 每行自带的 `attributionSkill` | 真值 |
| 调用耗时 | `tool_use` 所在行时间戳 → 对应 `tool_result` 行时间戳（`sourceToolAssistantUUID` 关联） | 真值 |
| API token | 每次 API 响应的 `usage`，按 `(message.id, requestId)` 去重（与 /tokens 同一契约 I5） | 真值 |
| 失败 | `tool_result.is_error`；Bash 另有 `interrupted` | 真值 |
| 系统事件 | `compact_boundary`、模型 / effort 切换、`invoked_skills`、hook attachment、`edited_text_file` | 真值 |

## 3. 诚实边界（三级，界面上一律可见）

- **真值**：层级、耗时、API usage 汇总、skill 归属、失败、phase。
- **估算 ≈**：调用级「发起成本」= 发出它的那次响应的 output ÷ 该响应里的调用数；「结果体积」= 返回内容按字符估算的 token。
- **推断（带标签）**：阶段标注、MCP 数据依赖（参数里出现了前一个调用结果中的 ID/值）、重试与原地打转。
- **已知盲区（不假装）**：permission 等待和工具执行在 transcript 里分不开，只有 `AskUserQuestion` / `ExitPlanMode` 算「等你」；思考块只有约 13% 非空，其余不显示；「异常慢」样本 < 20 次的工具不打标，不瞎猜。

**耗时三段算法**（S1 review 后修订）：机器执行 = 各调用区间的**并集**（并行不重复计）；等你 = 上述人工等待区间；模型生成 = 相邻活动行相隔 ≤ 15 分钟连成的区间减去前两者——**更长的空档算空闲，不算任何一段**（旧口径「墙钟 − 前两者」在真实数据上把一个 18 分钟的任务报成了 40 小时）。跨度 = 任务真实起止，活跃 = 三段之和，差值即空闲。

## 4. 技术落点（我来定，这里只记结论）

- 新模块 **`tokmon/trace.py`**：只读，只 import stdlib + `discovery` + `project`（与 activity 同级的独立支柱，P4）。只产出 token 分项；**$ 在 serve 层用 `pricing` 换算**（I3 定价集中，不复制单价）。
- 解析：按文件 `(mtime, size)` 缓存；**惰性**——任务列表只扫主会话，点开某个任务才解析它的 subagent / workflow 文件。
- 接口：`/api/workflow/tasks` · `/api/workflow/task` · `/api/workflow/stats`（S3）。
- **新页面放独立文件 `tokmon/pages/workflow.html`**，不再往已 2000+ 行的 `serve.py` 里内联（侧批 #6）。
- 分析函数（摘要、耗时三段、归因、异常识别）写成纯函数，单测钉死。

## 5. 切片与完成判据

| 切片 | 内容 | 完成判据 |
|---|---|---|
| **S1 回放主干** | trace 数据层 · 左栏任务列表 · L0 摘要 · 树 + 瀑布 · L2 明细 · 异常高亮 · 穿插内容 · 导航清理 · /tokens 深链 | ① 近 7 天任选一个任务都能完整回放；② **任务 token 真值与 /tokens 对账一致**（抽 3 个任务）；③ 含 workflow 的任务 phase / agent 层级完整；④ 单测覆盖纯函数；⑤ **跑一次多 agent 对抗式 review 并修掉确认项** |
| **S2 学习层** | 名词悬浮 · Workflow 脚本对照 · 阶段标注（推断）· MCP 数据依赖（推断） | 推断项全部带标签；脚本 phase 与回放 phase 一一对应 |
| **S3 统计子页** | 工具 / skill 排行 · MCP 健康面板 · 跨任务对照 | 排行数字与回放逐项可追溯 |

每刀独立可用，**你验收后再进下一刀**。

## 6. 明确不做

- ❌ 不调模型生成摘要或解读（确定性优先；语义层仍按 MISSION_CONTROL §9 推迟）。
- ❌ 不写 `~/.claude` 下任何东西（I1）。
- ❌ 不做推送：只有选中任务在跑时 5s 轮询，没有 websocket。
- ❌ 不碰远程控制与通知（已按你的决定搁置）。
