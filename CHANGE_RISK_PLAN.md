# 改动与风险 · 一页规格

> **【状态章 · 2026-09-22】S1 改动清单 ✅ 已交付（0.13.0），待你验收 → S2 风险规则。**
>
> 来源：2026-09-22 五轮问卷（19 项决定）+ 已交付的 `trace` 支柱（`/workflow` S1–S3，v0.10.0 → v0.12.0，全部验收）。
> 刻意只写一页：决策 + 边界 + 判据。实现细节在代码与单测里，不在这里（沿用 [WORKFLOW_TAB_PLAN.md](WORKFLOW_TAB_PLAN.md) 的纪律）。

## 0. 为什么是它

[MISSION_CONTROL.md](MISSION_CONTROL.md) 的北极星要回答四件事：**推进 / 卡住 / 烧钱 / 乱改**。
前三格分别有 `/sessions`、`/workflow`、`/tokens` 顶着；「乱改」是文档自己写的**四象限里最弱的一格**（§backlog 风险事件）。

而 `trace` 支柱已经把每一次 `Edit` / `Write` / `Bash` 连同文件、参数、所属任务都解析好了——
做这件事**不需要新数据源**，只需要在已有的构建结果上再算一层。

顺带补上 `/workflow` 的一个缺口：回放能告诉你「它是怎么做的」，但不能一眼告诉你**「它到底改了什么、改完验证了没」**。

## 1. 要回答的三个问题

1. 这次任务**到底改了什么**？→ 改动清单（S1）
2. 这中间有没有**危险或打转**的动作？→ 风险标记（S2）
3. 这些**在变好还是变差**？→ 趋势 + 实时信号（S3）

## 2. 决策表（五轮问卷，19 项）

| 维度 | 决定 | 口径 / 备注 |
|---|---|---|
| 放在哪 | **融进现有页面，不加新页面** | 回放摘要加「改动」块 · 统计页加「风险」段 · 任务列表加角标 · `/sessions` 加实时角标 |
| 统计页结构 | **顶部分段导航** | 概览 / 工具 / Skill / MCP / 风险 / 对照 / 趋势；仍是一整页，滚动时高亮当前段 |
| 顶部导航 | 体检 / 回测 / 厂商账单**收进「更多」** | 常驻：主页 · Session 状态 · Token 看板 · 工作流 · 进程监控 |
| 改动来源 | `Edit` / `Write` / `NotebookEdit` = **真值**；Bash 命令里的改动 = **推断**（标出来） | 推断认 `sed -i`、重定向写文件、`mv`、`rm`、`git checkout` 等 |
| 清单内容 | 改了几次 · 改了多少行 · 改完验证了没 · 改动时间线 | **分层披露**：默认一行汇总 + 文件表，时间线点开才有 |
| 文件反查 | **左栏搜索框直接搜文件名 / 路径片段** | 命中的任务行标出「改了 N 次」 |
| 风险规则 | 打转类 · 大改动 / 越界 · 破坏性操作 | 敏感文件（`.env` / 密钥 / CI 配置）**这次不做** |
| 分级 | **两级：提示 / 警示** | 精确率审过之前**一律只做提示**；事件总线对应 Info / Warning，**不发 Critical** |
| 「反复改」 | 「改 → 验证失败 → 再改同一文件」**≥ 3 轮** | 一口气分多次写一个大文件**不算** |
| 「大改动」 | **绝对阈值**，再用真实数据校准到约 **5%** 命中 | 阈值写进悬浮提示，页面上可见 |
| 「越界」 | 以**任务的工作目录**为界 | 跨仓改动也会被标出来——已知会有点吵，接受 |
| 实时信号 | `/sessions` 行内角标 + 点击进实时回放；**事件写进总线** | 通知仍关（你 2026-09-21 的决定），以后想开只需配置 |
| 趋势形式 | **「vs 上一周期」+ 行内小趋势线** | 不加独立图表区 |
| 趋势指标 | 质量 · 风险 · 效率 · 外部依赖 | 失败率 / 打转、风险数 / 未验证比例、每任务 token 与活跃时长、MCP 失败率 |
| 趋势粒度 | **跟着时间范围** | 今天按小时 · 近 7 天按天 · 近 30 天与全部按周 |
| 顺手打磨 | 失败原因归类 · 「偏慢」按命令族 | 冷启动缓存 / `serve.py` 拆模块 / atlas 更新**这次不做** |
| 顺序 | **S1 改动 → S2 风险 → S3 趋势 + 实时** | 每刀独立可用，你验收后再进下一刀 |
| review | **S2 之后**跑一轮多 agent 对抗式 review | S1 自查 + 单测；风险规则误报代价最高，review 对准它 |
| 改动判据 | 拿 `git commit` **对账**，报覆盖率 | 漏的如实报（预计漏在 Bash 侧） |

## 3. 口径与诚实边界

沿用 `/workflow` 的三级：**真值 / 估算（≈）/ 推断（写明）**。具体到这一版：

- **改动行数**（S1 实现时收紧了）：工具返回里带 `structuredPatch` 时按**实际落盘的补丁**数 +/− 行（真值，实测约 29% 的
  `Edit` / `Write` 有）；没有就退回按 `old_string` / `new_string` 算（请求的改动）。`replace_all` 替换了几处不知道、
  `Write` 覆盖前多大不知道 -> 数字标 `?` 说明是**下限**；Bash 侧的改动**不给行数**，只给「改过」。
- **「改完验证了没」**：最后一次改动之后有没有「验证」阶段的调用（复用 S2 的阶段标注，**推断**）。
  你在页面之外手动跑的测试**看不见**，界面上标明这一点。
- **打转**：复用 S2 的阶段段落——「修改 → 验证 ✗ → 又改同一个文件」记一轮，≥ 3 轮才标。
- **越界**：与任务提问时的 `cwd` 比；子 agent 用它自己的 `cwd`。
- **只标不拦**：全程只读，绝不阻止、不回滚、不自动干预（`I1` + MISSION_CONTROL 硬不变量「监控 ≠ 干预」）。
- **误报纪律**：硬不变量是「误报零容忍」。所以每条规则先只做**提示**，在真实数据上逐条审完精确率、
  并把规则原文写进悬浮提示之后，才允许升为**警示**；升不上去的就一直是提示。
- **趋势**：桶按所选时间范围切；「vs 上一周期」= 同样长度的上一段窗口（沿用 `/tokens` 的自基线口径）。
  样本太少的桶**标出来**，不画成趋势让人误读。

## 4. 技术落点（结论，不写实现）

- 数据层仍在 **`tokmon/trace.py`**（P4：只 import stdlib + `discovery` + `project`）。改动清单与风险标记都是
  **构建结果之上的纯函数**，跟 `build_task` 一起出；数字照 S3 的办法进「数字 → 明细」索引，**同样逐项可追溯**。
- 事件走 **`events` 总线**，类型名沿用 MISSION_CONTROL §4 的 `REPEATED_FILE_EDIT` / `LARGE_DIFF` / `ERROR_SPIKE`，
  破坏性操作新增一类；**监控层绝不直接调通知**（硬不变量）。
- 页面在 **`tokmon/pages/workflow.html`** 内扩展；`/sessions` 只加角标 + 深链，**不动它的状态推断**。
- 接口：`/api/workflow/stats` 增加 `risk` / `trend` 字段，`/api/workflow/drill` 增加改动与风险的索引键；
  **不新增页面路由**；两者照旧过读门 + 脱敏。
- 性能：趋势与风险都在**已有的那一遍构建**里算完，不额外扫盘；统计接口仍走 30 秒缓存。
  冷启动缓存这次不做 → **实测耗时写进判据与 CHANGELOG**，不藏。

## 5. 切片与完成判据

| 切片 | 内容 | 完成判据 |
|---|---|---|
| **S1 改动清单** ✅ 0.13.0 | 回放摘要「改动」块（次数 / 行数 / 验证 / 时间线）· 左栏按文件反查 · 统计页「改动」小节 | ① 清单每一条都能跳回回放里那次调用 ✅；② 对有 `git commit` 的任务拿提交对账，**报出覆盖率**，漏的归因清楚 ✅（仓库汇总 81.2%，本仓库 55.7%，原因写进 CHANGELOG）；③ 行数与「验证了没」的口径有单测钉死 ✅ |
| **S2 风险规则** | 三类规则 + 两级 + 回放徽章 / 列表角标 / 统计「风险」段 | ① 每条规则单测钉死；② 真实数据逐条审误报，**报每条的精确率**；③ 命中率校准到「少而准」（大改动约 5%）；④ **跑一轮多 agent 对抗式 review 并修掉确认项** |
| **S3 趋势 + 实时** | 「vs 上一周期」+ 行内趋势线（四类指标）· `/sessions` 角标 + 事件总线 · 顺手打磨两项 | ① 趋势数字与同窗口的汇总自洽（抽查对账）；② 在**真实运行中**的会话上看到角标并能点进实时回放；③ 统计接口实测耗时写进 CHANGELOG |

## 6. 明确不做

- ❌ **敏感文件规则**（`.env` / 密钥 / CI 配置）——这轮你没选；等风险规则的精确率审完再议。
- ❌ **拦截 / 回滚 / 自动干预**：只标，不拦。
- ❌ 独立新页面（`/risk` 之类）。
- ❌ 冷启动缓存 · `serve.py` 拆模块 · atlas 图谱更新——这次不做，记在债里。
- ❌ 调模型生成摘要或解读（语义层仍按 MISSION_CONTROL §9 推迟）。
- ❌ 写 `~/.claude` 下的任何东西（I1）。
