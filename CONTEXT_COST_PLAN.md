# 省钱：上下文体检 · 一页规格

> **【状态章 · 2026-09-26】方向已定（你在三个方向里选了它）· S1 进行中。**
>
> 来源：2026-09-26 的方向选择 + 近 7 / 14 天真实数据的摸底（下表，只写比例）。刻意只写一页：决策 + 边界 + 判据。
> 实现细节在代码与单测里（沿用 [SESSIONS_COCKPIT_PLAN.md](SESSIONS_COCKPIT_PLAN.md) 的纪律）。

## 0. 为什么是它

北极星的「烧钱」一格，到 0.18.0 为止做到了**看得见**（`/tokens` 一屏分析台、`/sessions` 每行的等价 $）。
下一步是**知道怎么省**。摸底数据说钱主要不是花在「说了什么」上，而是花在「每轮都要把整段上下文再读一遍」上：

| 近 7 天（全部来源，按等价成本） | 占比 |
|---|---|
| 缓存读（每轮把上下文再读一遍） | 59% |
| 上下文超过 30 万 token 的回合 | 48% |
| **上下文税**：每轮上下文超过 15 万的那部分的读取费 | **27.6%** |
| 同上，超过 30 万的那部分 | 13.1% |
| **离开超过 1 小时回来**，整段上下文重写缓存比命中多花的 | **5.3%** |

**缓存有效期的推断先回测过**（近 14 天主线程）：Claude Code 几乎只写 **1 小时**缓存（8638 次 1 小时 / 2 次 5 分钟）；
「空了超过 1 小时 → 下一轮重建」的**精确率 96.7%**（118/122）。另有 52 次重建发生在 1 小时之内（多半是中途换模型、上下文变了），
时间解释不了——这类只如实计数，不当成省钱机会。

## 1. 三刀

| 切片 | 内容 | 完成判据 |
|---|---|---|
| **S1 看得见上下文**（0.19.0） | `/sessions` 每行：上下文多大（最近一轮送进模型的全部 token，真值）· 每轮 ≈$ · 超过 15 万那部分每轮多少；**等你的会话**显示缓存还剩多久 / 已过期、下一轮重建 ≈$（命中只要多少）；页顶「大上下文会话 N」 | ① 行里的上下文与该会话 transcript 最后一次主线程响应的 usage **逐个对账全等**；② 缓存推断的回测数字写进 CHANGELOG；③ `/sessions` 刷新耗时不退化 |
| **S2 省钱机会**（0.20.0） | `/tokens` 的数据立方加两项：上下文税（15 万以上）· 离开超 1 小时的重建多花；一张「省钱机会」卡片**跟着图上的筛选走**（点一个会话就看它的）；按会话排行，点进回放 | ① 上下文税 ≤ 缓存读费、重建多花 ≤ 缓存写费，全窗口对账；② 与摸底脚本在同一窗口数字一致；③ 立方耗时写进 CHANGELOG |
| **S3 上下文是怎么涨起来的**（0.21.0） | `/workflow` 回放：这个任务的上下文曲线（每轮真值）+ 每次大涨归到它前面的调用（按结果体积估算）+ 压缩 / 清空处标出来；「撑大上下文的前几步」；铃铛可选提醒「正在跑的会话上下文刚过 30 万」（默认关） | ① 曲线上每个点与该轮 usage 全等；② 归因覆盖率（被归到具体调用的增长 ÷ 总增长）报出来；③ 真实任务截图核对 |

## 2. 决策

| 维度 | 决定 | 备注 |
|---|---|---|
| 放在哪 | 融进 `/sessions`、`/tokens`、`/workflow`，不加新页面 | 与前两轨一致 |
| 「上下文」口径 | 一轮送进模型的全部输入 = 新输入 + 缓存读 + 缓存写（真值） | 主线程与子 agent 各算各的；`/sessions` 行里只看主线程 |
| 上下文税的参考线 | **15 万**（固定，写在悬浮提示里） | 摸底：15 万以上占 27.6%；30 万偏紧，10 万对长任务不现实 |
| 缓存有效期 | 看这个会话最近一次**写**的是哪种（实测几乎都是 1 小时） | 精确率 96.7% 的推断，写「推断」 |
| 建议怎么说 | **只给事实和算得出的账**：「超过 15 万的部分每轮 ≈$X」「离开超过 1 小时，下一轮重写 ≈$Y」 | 不替你判断压缩会不会丢信息；过期之后连 `/compact` 也要重读整段——所以「要走很久，趁缓存还在先压缩」才是有用的时机 |
| 提醒 | 只有 S3 的「上下文刚过 30 万」一条，**默认关** | 缓存快过期**不提醒**：「等你已久」的会话大多是你已经不打算回去的，会变成噪音 |
| 隐私 | 仓库是公开的 | 文档与 CHANGELOG 只写比例与计数，不写真实花费、项目名 |

## 3. 口径与诚实边界

- 等价 $ 与 `/tokens` 同一套单价，写「≈」（估算，不是账单）；订阅用户的 $ 是用量价值，不是真实扣费。
- 「重建多花」= 这一轮缓存写的钱 − 同样多 token 按缓存读算的钱（假设它本来能命中）。
- 缓存状态只对**主线程**推断；子 agent 的缓存各自独立，不在行里显示。
- 全程只读：不替你压缩、不改 Claude Code 设置（MISSION_CONTROL 硬不变量「监控 ≠ 干预」）。

## 4. 明确不做

- ❌ 自动压缩 / 自动开新会话 / 改 Claude Code 的缓存设置。
- ❌ 预测「压缩后回答质量会不会变差」——拿不到真值。
- ❌ 缓存快过期的推送提醒（理由见上）。
