# `/sessions` 过滤器 v2 · 诊断与改动计划

> 给主程序的实现交接。**设计与约束，不是代码。**
> 承接 [SESSIONS_FILTER_PLAN.md](SESSIONS_FILTER_PLAN.md)（v1 已交付）。本文件回答用户实测截图暴露的问题。
> **v1 把「怎么筛」做对了（状态源、持久化、chips 不丢选中都实现得很好），但「筛出来的东西对不对、诚不诚实」错了。**

> **四个已敲定的决定（2026-07-13）：**
> 1. **「空闲」升级为第 6 个独立状态**，**彻底删除「隐藏空闲会话」复选框**。（用户授权我决定，理由见 §2.1）
> 2. **计数改为 faceted** —— chip/pill 上的数字 = 点它会真的出多少条。
> 3. **列表支持 卡片 / 表格(data-frame) 切换**。
> 4. **默认不筛，显示全部 159。**

---

## 1. 🔍 诊断：两张截图的成因（可用算术精确复现）

用户实测：
- 截图 A：筛 `状态=等你` → pill 明明写着 **等你 81**，结果 **显示 0/159**。
- 截图 B：筛 `项目=alyssa rent agent` → chip 明明写着 **36**，结果 **显示 2/159**。

**这不是筛选逻辑写错了，是三个设计缺陷叠加。**

### 根因 1 · 「空闲」不是状态，是「等你」的**子标记**

`activity.py` 中 `idle` 只在 `AWAITING_USER` 分支被赋值（`age >= idle_after_s`，即 10min）。
所以 **`空闲 ⊆ 等你`**，二者**不是并列关系**。

实测数据坐实了这一点：**等你 81、空闲 81** —— **这 81 个「等你」的会话全部超过 10 分钟，无一例外。**
即：**在真实数据里，「隐藏空闲会话」≡「隐藏全部『等你』」。**

而 pill 那一行（`serve.py:1304-1312`）把 `活跃 / 久未返回 / 等你 / 空闲 / 已关闭 / 读不出` **并排渲染成同一个维度**。
但真实划分是 `81(等你) + 72(已关闭) + 6(久未返回) = 159 = 总数` —— **`空闲 81` 是对「等你」的重复计数**。
**一个「互斥状态轴」里混进了一个「非互斥标记轴」。** 这是所有混乱的源头。

### 根因 2 · 「隐藏空闲」是个**隐形过滤器**

- 它**确实在生效**：`applyFilters` 的 `serve.py:1269`。
- 但构建「过滤中」提示的 `bits`（`serve.py:1328-1331`）**只收 项目 / 状态 / 时间窗**，
  **不含 `hideIdle`，也不含文本搜索**。
- 更糟：`shownNote`（`serve.py:1369`）**只在 `!bits.length` 时才渲染** ——
  **一旦你开了任何 filter，连原来那句「已隐藏空闲/不匹配」都消失了。**

### 🎯 于是精确复现

| 截图 | 过程 | 结果 |
|---|---|---|
| **A** `状态=等你` | 状态筛留下 81 个 `AWAITING_USER` → `hideIdle`（**默认勾选**）把这 81 个**全部**杀掉 | **0/159**，且 UI **零线索**说明是谁干的 |
| **B** `项目=alyssa(36)` | 其中 34 个是「等你+空闲」→ 被 `hideIdle` 吃掉 → 只剩 2 个「久未返回」 | **2/159** |

### 根因 3 · 所有计数都在**全量**上算，不反映当前 filter

- pill 数字来自服务端快照 `d.counts`（全量，`serve.py:1298`）。
- chip 的 `projCounts` 来自 `d.sessions`（全量，`serve.py:1316`）。

**→ chip 承诺「36」却给你 2；pill 承诺「等你 81」却给你 0。**
**数字在承诺一件筛选并不兑现的事** —— 这正好违反本项目的**原则 1（宁可显示未知，也不要悄悄误导）**。
只不过这次误导人的，是过滤器自己。

---

## 2. 改动计划

### 2.1 【P0】状态轴重构 —— 一招同时消灭根因 1 和 2

**决定（用户授权我定）：把「空闲」升级为第 6 个独立、互斥的状态组；彻底删除「隐藏空闲会话」复选框。**

新的**状态轴（严格互斥、完全覆盖）**：

| 组 | 判定 | 说明 |
|---|---|---|
| `live` 活跃 | `WORKING` \| `PROCESSING` | |
| `wait` 等你 | `AWAITING_USER` **且 `!idle`** | 刚回到等你（< 10min） |
| `idle` 空闲 | `AWAITING_USER` **且 `idle`** | 等你但已超 10min |
| `amb` 久未返回 | `AMBIGUOUS_PENDING` | |
| `closed` 已关闭 | `CLOSED` | |
| `unk` 读不出 | `UNKNOWN` | |

**关键点：**
- `groupOf()` 的入参必须从 **`state` 改成整个 `session`**（因为 `idle` 是独立字段，光看 `state` 分不出 wait / idle）。**这是本次最容易漏改的地方。**
- **删掉 `#hideidle` 复选框。** 不想看空闲 → 不选它 / 选别的。**隐形过滤器问题随之自动消失。**
- 「后台在跑」(`c.background`) **不是状态，是标记**（一个 WORKING 会话可以同时在跑后台任务）。
  **保持只读展示，明确别把它塞进状态轴** —— 否则就是重蹈根因 1 的覆辙。

#### 🩺 送一个自检不变量（很符合本项目的 doctor 精神）

新状态轴是**严格划分**，所以必然满足：

```
活跃 + 等你 + 空闲 + 久未返回 + 已关闭 + 读不出  ==  总数
```

**在 UI 上加一条断言**：若六组之和 ≠ `total`，说明 `activity.py` 新增了一个状态而前端没跟上
→ **显式警告**（如「⚠ 状态划分不完备，有 N 个会话未被任何状态组覆盖」），**而不是悄悄把它们漏掉**。
这正是这个项目一贯的「让漂移可被发现」——只不过这次是**前后端状态机的漂移**。

### 2.2 【P0】计数改为 faceted —— 让数字不再撒谎

**语义**：每个 chip / pill 上的数字 = **在「其它轴」的 filter 之下，点它会真的出多少条**。

- **项目 chip 计数** = 先应用 `状态 + 时间窗 + 文本` 过滤，再按 project 分组计数（**忽略项目轴自己的选择**）。
- **状态 pill 计数** = 先应用 `项目 + 时间窗 + 文本` 过滤，再按状态组分组计数（**忽略状态轴自己的选择**）。

这是标准的分面搜索（**轴内 OR、跨轴 AND**）行为。

**它带来的硬保证（也是验收判据）：**
> **在该轴未选任何项时，点一个显示 N 的 chip/pill，结果行数必须恰好是 N。**

这一条成立，两张截图的问题就**从根上不存在了**。

**顺带**：`共 159` 改成 `共 159（当前显示 N）`。

### 2.3 【P0】「过滤中」提示补全 —— 不许再有隐形 filter

- `bits` 必须收录**所有**生效的过滤条件，**包括文本搜索**（现在它是隐形的，`hideIdle` 删了但文本还漏着）。
- 「显示 N/159」+「清除全部过滤」**只要有任一 filter 生效就必须可见**。
- 保留 v1 已做对的两条诚实提示：**选中项目本帧无会话**（不静默变全部）、**时间窗排除了「时间读不出」的会话**。

### 2.4 【P0】默认视图 = 不筛，显示全部 159

- 删除 `hideIdle` 后，默认即「什么都不筛」。
- localStorage 里遗留的旧 `FILTERS`（含 v1 的状态组名）需**兼容处理**：
  旧值里没有 `idle` 组，**不会崩**，但要确保读进来的 `states` 仍被 `STGRP_LABEL` 白名单校验（v1 已有此保护，`serve.py:1258`，**保留**）。

### 2.5 【P1】列表形态：卡片 / 表格(data-frame) 可切换

**默认表格**（因为决定 4 是「默认显示全部 159」——159 张卡片没法扫，表格才能兑现北极星的「5 秒看完」）。

**表格列**：`项目 | 标题 | 状态 | 模型 | 最后活动`（可选 `分支`）
- 状态列用彩色徽章（复用现有 `.sb` 样式）。
- **列头可点排序**（默认按「最后活动」降序）。
- **行可展开** → 展开后就是现在的卡片内容（当前步骤 / 事件 chips）。这样两种视图不是两套渲染，而是**同一份数据的两级详略**——避免维护两套逻辑腐烂。
- 切换开关的选择**记进 localStorage**（同 FILTERS）。

### 2.6 【P2】UI 重塑：下拉分区 + 气泡式（用户说「可以到时再调整」）

- 把「项目 / 状态 / 时间窗」收进 **2–3 个分区的下拉菜单**，选中项以**气泡（chips）**形式回显在下拉外面，可单个 × 删除。
- **不在本轮做**，但上面的状态模型（`FILTERS` 单一真相源）已经能直接支撑它——**只换外观，不换内核**。

---

## 3. 改动范围（依然很小）

| 文件 | 改动 |
|---|---|
| `serve.py` 的 `SESS_PAGE` | **全部改动都在这里**（HTML + JS） |
| `activity.py` / `/api/sessions` / 内核 | **零改动** ✅ |

**为什么后端依然不用动**：`d.sessions` 里每条会话都自带 `state` 和 `idle` 字段，
前端能自己算出全部 6 个状态组和所有 faceted 计数。服务端的 `d.counts` 从此**只作交叉校验，不再驱动 pill**。
**I6（展示层不反向污染内核）继续守住。**

---

## 4. 验证（本片仍无单测 —— 诚实记录）

同 v1：纯前端 JS，项目无 JS 测试设施。**不假装有覆盖。** 补偿：

1. `groupOf` / `applyFilters` / `facetCounts` 保持**纯函数**。
2. **可眼验的硬判据**（下面 §5 的第 1、2 条）——它们比单测更直接。
3. §2.1 的**自检不变量**（六组之和 == 总数）本身就是一个跑在 UI 里的运行时断言。

---

## 5. 完成判据

1. **🎯 点任意一个 chip/pill（该轴未选其它项时），出来的行数 == 它上面显示的数字。**
   → 这一条成立，两张截图的问题即彻底消失。
2. **筛 `状态=空闲` 能出 81 条；筛 `项目=alyssa rent agent` 能出 36 条。**（用户的原始期望，直接验）
3. **不存在任何隐形过滤器** —— 凡是在收窄结果的东西，都出现在「过滤中」提示里（含文本搜索）。
4. **六个状态组计数之和 == 总数**；不等时显式警告，不静默漏会话。
5. 默认打开 = 不筛、全部 159、表格视图。
6. **`git diff` 只碰 `serve.py` 的 `SESS_PAGE`。**

---

## 6. ⚠️ 一个与本任务无关、但更紧急的发现

用户截图里的地址是 **`https://judge-attitudes-technological-couple.trycloudflare.com/sessions`** ——
**看板已经挂在公网 cloudflared 隧道上了。**

而 `/sessions`、`/tokens`、`/processes`、`/api/*` 这些**读页目前完全没有鉴权**
（`_ctl_guard` 只保护控制端点）。**任何拿到这个 URL 的人，都能看到你全部会话的标题、当前步骤、思考片段、项目名和 token 花费。**

这正是 [REMOTE_CONTROL_PLAN.md](REMOTE_CONTROL_PLAN.md) §6 里那个**尚未实施**的 `MC_REMOTE` 收口
（开启后所有端点含读页都要 token；无 token 则拒绝远程启动）。

**建议：要么尽快做 `MC_REMOTE`，要么先把隧道关掉。** 这件事的优先级高于本文件的任何一条。

---

## 收尾：沿用既有纪律

`design → implement → 真机冒烟 → 对抗式 review → 修确认项`。
先问北极星：**这让「我一眼看出谁在推进 / 卡住 / 等我」更快、更准、更可信了吗？**
—— v1 让它更快了，但**让它更不可信了**（数字撒谎）。v2 的全部意义是**把「可信」补回来**：
**过滤器自己，必须先是诚实的。**
